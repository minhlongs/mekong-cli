# ci/config/osv-scanner.toml — OSV Scanner scope config
# Docs: https://google.github.io/osv-scanner/configuration/

[IgnoredVulns]
# Format:
# [[IgnoredVulns]]
# id = "GHSA-xxxx-xxxx-xxxx"
# reason = "Transitive only, no direct exposure. Review by: YYYY-MM-DD"
#
# Add entries here when osv-scanner flags transitive deps we cannot fix.
# Each entry MUST include a reason + review-by date.

# Example (remove when real entries exist):
# [[IgnoredVulns]]
# id = "GHSA-placeholder-0000-0000"
# reason = "Transitive dep in test toolchain only. Review by: 2026-07-01"
