---
name: gate_check
description: Infer owner/repo from git remote origin URL — handles https + ssh.
allowed-tools: [Bash, Read, Write, Edit, Grep, Glob, WebFetch]
argument-hint: ""
---

# /gate_check

Infer owner/repo from git remote origin URL — handles https + ssh.
