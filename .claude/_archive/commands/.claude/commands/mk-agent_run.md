---
name: agent_run
description: Spawn an agent with *task* and emit a structured result.
allowed-tools: ['Bash', 'Read', 'Write', 'Edit', 'Grep', 'Glob', 'WebFetch']
---

# /agent_run

Spawn an agent with *task* and emit a structured result.

