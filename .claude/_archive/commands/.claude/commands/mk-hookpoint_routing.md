---
name: hookpoint_routing
description: Return registered hooks for a point (sorted by priority).
allowed-tools: [Bash, Read, Write, Edit, Grep, Glob, WebFetch]
argument-hint: ""
---

# /hookpoint_routing

Return registered hooks for a point (sorted by priority).
