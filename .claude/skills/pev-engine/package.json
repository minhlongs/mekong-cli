{
  "name": "pev-engine",
  "version": "1.0.0",
  "description": "Plan-Execute-Verify engine for ClaudeKit — TypeScript port of Mekong CLI PEV",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit --strict",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
