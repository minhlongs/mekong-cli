# 🏯 BINH PHÁP: CROSS-FLOW INTEGRATION MAP

> **"合縱連橫"** - Vertical and horizontal alliances

---

## 📊 SYSTEM INTEGRATION ARCHITECTURE

```
┌─────────────────────────────────────────────────────────────────┐
│                    👤 CHỦ TỊCH (Chairman)                       │
│                 Antigravity CLI (port 8080)                     │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                   🏯 ANTIGRAVITY PROXY                          │
│                     Port: 8045 (to enable)                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ Model Routing:                                           │   │
│  │ • internal-background-task → claude-opus-4-5-thinking    │   │
│  │ • scheduling.mode = "Balance"                            │   │
│  │ • Flash Attention: 2.49x-7.47x speedup                   │   │
│  └─────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
┌───────────────┐  ┌─────────────────┐  ┌────────────────┐
│ CLAUDE-FLOW   │  │   CLEO          │  │  AgencyOS      │
│ V3 Swarm      │  │   Task Mgmt     │  │  Swarm         │
│ 15 agents     │  │   v0.68.0       │  │  4 agents      │
│ Port: 3000    │  │                 │  │                │
└───────┬───────┘  └────────┬────────┘  └───────┬────────┘
        │                   │                   │
        └───────────┬───────┴───────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                       🏭 FACTORY                                │
│              fullstack-developer agents                         │
│         /delegate → CTO Division (60%)                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔗 CROSS-FLOW MAPPING

### Flow 1: Task Management

```
User Request → CLEO → claude-flow task → Agent Pool → Execution
```

### Flow 2: Factory Production

```
/delegate → CC CLI → Antigravity Proxy → Factory Agent → Product
```

### Flow 3: Multi-Agent Coordination

```
Complex Task → swarm coordinate → 15 agents → consensus → Result
```

### Flow 4: Background Processing

```
Long Task → Antigravity Proxy → claude-opus-4-5-thinking → Async
```

---

## ⚙️ CONFIGURATION NEEDED

### 1. Antigravity Proxy (enable)

```json
{
    "proxy": {
        "enabled": true,
        "auto_start": true,
        "port": 8045
    }
}
```

### 2. Claude-Flow MCP (enable)

```json
{
    "mcpServers": {
        "claude-flow": {
            "autoStart": true
        }
    }
}
```

### 3. Swarm Start

```bash
claude-flow swarm start
```

---

## 📋 INTEGRATION COMMANDS

```bash
# Start all systems
claude-flow daemon start          # Background workers
claude-flow swarm start           # 15-agent mesh
cleo list --human                 # Task tracking

# Check status
claude-flow status               # System overview
claude-flow swarm status         # Swarm metrics
cleo list                        # Tasks JSON
```

---

**Created:** 2026-01-26
**Integration:** Binh Pháp ĐIỀU 14: CROSS-FLOW INTEGRATION
