---
description: "Debug applications via the mk platform — error analysis, stack traces, root cause investigation, and fix suggestions."
argument-hint: "[args]"
allowed-tools: Bash
---
# /mk:debug

Debug applications via the mk platform — error analysis, stack traces, root cause investigation, and fix suggestions.

## Execution
```bash
python3 -m src.main mk debug "$ARGUMENTS"
```
