---
description: "Deploy phase: verify gates → ship/hold"
argument-hint: "[args]"
allowed-tools: Bash
---
# /mk-mk-deploy

Deploy phase: verify gates → ship/hold

## Execution
```bash
python3 -m src.main mk mk-deploy "$ARGUMENTS"
```
