---
description: "RaaS Gateway synchronization: validate, register, track usage"
argument-hint: "[args]"
allowed-tools: Bash
---
# /mk:mk-sync-raas

RaaS Gateway synchronization: validate, register, track usage

## Execution
```bash
python3 -m src.main mk mk-sync-raas "$ARGUMENTS"
```
