---
description: "Launch and manage UI components via the mk platform — dashboard, TUI, and web interface controls."
argument-hint: "[args]"
allowed-tools: Bash
---
# /mk:ui

Launch and manage UI components via the mk platform — dashboard, TUI, and web interface controls.

## Execution
```bash
python3 -m src.main mk ui "$ARGUMENTS"
```
