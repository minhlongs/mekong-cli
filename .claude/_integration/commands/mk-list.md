---
description: "List resources via the mk platform — goals, tasks, agents, schedules, and registered tools."
argument-hint: "[args]"
allowed-tools: Bash
---
# /mk:list

List resources via the mk platform — goals, tasks, agents, schedules, and registered tools.

## Execution
```bash
python3 -m src.main mk list "$ARGUMENTS"
```
