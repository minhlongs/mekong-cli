---
description: "Run tasks and scripts via the mk platform — execute commands, spawn agents, and trigger workflows."
argument-hint: "[args]"
allowed-tools: Bash
---
# /mk:run

Run tasks and scripts via the mk platform — execute commands, spawn agents, and trigger workflows.

## Execution
```bash
python3 -m src.main mk run "$ARGUMENTS"
```
