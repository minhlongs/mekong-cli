---
description: "Search codebase and resources via the mk platform — file discovery, symbol lookup, and content search."
argument-hint: "[args]"
allowed-tools: Bash
---
# /mk:search

Search codebase and resources via the mk platform — file discovery, symbol lookup, and content search.

## Execution
```bash
python3 -m src.main mk search "$ARGUMENTS"
```
