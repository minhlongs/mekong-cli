---
description: 🎨 FRONTEND - Build pixel-perfect UI/UX (Binh Pháp: Hình Thế)
argument-hint: [ui task]
---

# /frontend - Frontend Developer

> **"Hình người không bờ bến, như nước chảy"** - Form is limitless, flowing like water.

## Usage

```bash
/frontend [action] [options]
```

## Actions/Options

| Action/Option | Description | Example |
|--------------|-------------|---------|
| `create` | Create a new component/page | `/frontend create "Navbar"` |
| `style` | Apply styles (Tailwind/CSS) | `/frontend style "Make buttons rounded"` |
| `fix` | Fix UI bugs or glitches | `/frontend fix "Mobile layout overflow"` |
| `--check` | Run UI validation (MD3) | `/frontend create "Card" --check` |

## Execution Protocol

1. **Agent**: Delegates to `frontend-developer` (or `ui-ux-designer`).
2. **Process**:
   - Reads `design-guidelines.md`.
   - Implements component using React/Next.js.
   - Ensures MD3 compliance (`ui-server`).
   - Verifies responsiveness.
3. **Output**: Polished UI components.

## Examples

```bash
# Create a dashboard layout
/frontend create "Admin Dashboard Layout with Sidebar"

# Fix responsive issue
/frontend fix "Header overlaps content on mobile"
```

## Binh Pháp Mapping
- **Chapter 4**: Hình Thế (Positioning) - Visual positioning defines user perception.

## Constitution Reference
- **MD3 Strict Mode**: Adhere to Material Design 3.

## Win-Win-Win
- **Owner**: High-quality, engaging interface.
- **Agency**: Reusable components.
- **Client**: Intuitive usage.
