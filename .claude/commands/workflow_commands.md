---
name: workflow_commands
description: Print AGI v2 planning hints (NLU + tools + reflection). Silently degrades.
allowed-tools: [Bash, Read, Write, Edit, Grep, Glob, WebFetch]
argument-hint: ""
---

# /workflow_commands

Print AGI v2 planning hints (NLU + tools + reflection). Silently degrades.
