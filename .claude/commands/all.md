---
name: all
description: Show complete status (system + api + quota + health)
allowed-tools: ['Bash', 'Read', 'Write', 'Edit', 'Grep', 'Glob', 'WebFetch']
---

# /all

Show complete status (system + api + quota + health)

