---
name: deps
description: List installed Python packages (invokes ``pip list`` via subprocess).
allowed-tools: ['Bash', 'Read', 'Write', 'Edit', 'Grep', 'Glob', 'WebFetch']
---

# /deps

List installed Python packages (invokes ``pip list`` via subprocess).

