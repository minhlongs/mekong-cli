---
description: 🚀 Startup Suite - Launch and funding commands
argument-hint: [:launch|:pitch|:deck|:vc]
---

## Mission

Unified startup command hub.

## Subcommands

| Command | Description | Core Module |
|---------|-------------|-------------|
| `/startup:launch` | Launch checklist | `startup_launcher.py` |
| `/startup:pitch` | Pitch preparation | `pitch_deck.py` |
| `/startup:deck` | Generate deck | `pitch_deck.py` |
| `/startup:vc` | VC readiness | `vc_hub.py` |

## Quick Examples

```bash
/startup              # Show startup status
/startup:launch       # Launch checklist
/startup:pitch        # Prepare pitch
/startup:deck         # Generate deck
/startup:vc           # VC metrics
```

---

🚀 **One suite. All startup operations.**
