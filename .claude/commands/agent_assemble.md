---
name: agent_assemble
description: Assemble a configured agent from a user goal (B6 Agent Factory).
allowed-tools: ['Bash', 'Read', 'Write', 'Edit', 'Grep', 'Glob', 'WebFetch']
---

# /agent_assemble

Assemble a configured agent from a user goal (B6 Agent Factory).

