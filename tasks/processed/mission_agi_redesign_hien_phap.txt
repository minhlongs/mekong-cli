/plan:hard "HIẾN PHÁP AGI REDESIGN — Tôm Hùm Autonomous System

## CONTEXT
You are working on the OpenClaw (Tôm Hùm) autonomous task execution system in /Users/macbookprom1/mekong-cli/apps/openclaw-worker/

Current files:
- apps/openclaw-worker/task-watcher.js (v21 — daemon that watches for mission files)
- scripts/tom-hum-dispatch.exp (expect script that holds CC CLI alive)

Current problems:
1. Idle detection triggers too early (25s idle = done, but some CC CLI thinking takes longer)
2. Expect script needs better ANSI handling for CC CLI output
3. No project-routing inside persistent session (always uses default_dir)
4. No mission result reporting (success/failure/what was done)
5. No session resume (if CC CLI crashes, loses all context)

## MISSION: REDESIGN FROM SCRATCH

Design and implement a production-grade autonomous system where:

### 1. PERSISTENT CC CLI SESSION
- CC CLI spawns ONCE and stays alive permanently (like Antigravity holds terminal)
- Missions are injected via typed commands, not new spawns
- Between missions, CC CLI idles at the prompt
- If CC CLI crashes then auto-respawn with --resume

### 2. RELIABLE COMPLETION DETECTION
- DO NOT rely on idle time alone — CC CLI thinking phases can be 60s+ idle
- Use MULTIPLE signals: prompt return + idle time + output analysis
- The prompt appears in ANSI escape sequences — handle them properly
- Consider: CC CLI status bar shows progress, parse it

### 3. PROJECT ROUTING
- Each mission specifies a target project (sophia, apex, 84tea, etc.)
- CC CLI should cd to project dir before executing
- Or use /add-dir to add project context without leaving session

### 4. MISSION RESULT REPORTING
- After each mission, write a structured result to /tmp/tom_hum_result.json:
  { status: 'success' or 'failure' or 'timeout', duration_s: N, summary: '...' }
- task-watcher reads this and logs it

### 5. SELF-HEALING
- If CC CLI process dies then respawn within 10s
- If expect process dies then task-watcher respawns it
- If task-watcher dies then launchd restarts it (create plist)
- Session resume using claude --resume session_id

### 6. CONSTITUTION COMPLIANCE
- All commands sent via /command format
- ClaudeKit-first workflow
- No premature stops
- Factory line execution
- Activity kick if stalled

## DELIVERABLES
1. Rewrite scripts/tom-hum-dispatch.exp — production-grade expect script
2. Rewrite apps/openclaw-worker/task-watcher.js — v22 persistent brain
3. Create apps/openclaw-worker/tom-hum.plist — launchd auto-restart
4. Update the Antigravity Constitution with a new article about Tôm Hùm autonomy

## CONSTRAINTS
- Node.js only (no npm install, no new dependencies)
- macOS expect (built-in, no tcl packages)
- Must work with Antigravity Proxy (port 8080)
- CC CLI model: claude-opus-4-6-thinking
- API key: sk-6219c93290f14b32b047342ca8b0bea9

/cook"
