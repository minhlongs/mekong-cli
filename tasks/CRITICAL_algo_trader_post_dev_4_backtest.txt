/plan:hard "
MISSION: Implement a comprehensive Backtesting Framework that can validate all 12 phases of the AGI trading system against historical market data. The framework must simulate realistic market conditions including slippage, latency, exchange fees, and on‑chain transaction costs. It should load historical orderbook data, trade prints, and blockchain state (from archives or snapshots) and execute all active strategies exactly as they would run in production. Output a detailed performance report with Sharpe ratio, max drawdown, win rate, profit factor, and month‑by‑month breakdown. All code must be in TypeScript with 100% Jest test coverage (using mock data). Provide a configuration file `config.backtest.json`.

**MODULE: Historical Backtest Engine**
- File path: `apps/algo-trader/src/testing/backtest/`
- Subcomponents:
  - `dataLoader.ts`: Loads historical market data from multiple sources:
    - CEX: Binance/Bybit spot and perpetual orderbook snapshots (Level 2) and trade history (Parquet/CSV format). Supports date‑range selection.
    - DEX: On‑chain swap events and pool states from Ethereum/Solana archives (via The Graph or direct RPC dumps).
    - Oracle: Historical price feeds for RWA (simulated from Chainlink data).
    - Mempool: Recorded pending transactions for MEV simulation.
    - Social/News: Archived sentiment scores from Phase 2/4 modules.
  - `simulationEngine.ts`: Replays data tick by tick, advancing time and feeding events to the core engine. It must:
    - Simulate exchange latency (configurable).
    - Apply realistic slippage based on orderbook depth at time of execution.
    - Deduct trading fees (maker/taker) per exchange.
    - Handle on‑chain transaction costs (gas) and block times.
  - `strategyLoader.ts`: Dynamically loads all active strategies from each phase (based on config) and initializes them with their historical parameters. Supports both live strategies and backtest‑only variants.
  - `stateManager.ts`: Maintains portfolio state (balances, open orders, positions) across all exchanges and chains. Updates after each simulated fill.
  - `metricsCollector.ts`: Records every trade, PnL, and risk metric. Computes:
    - Daily/ monthly returns
    - Sharpe ratio (risk‑free rate configurable)
    - Maximum drawdown
    - Win rate, average win/loss
    - Calmar ratio, Sortino ratio
    - Exposure by asset/exchange
  - `reportGenerator.ts`: Produces an interactive HTML report with:
    - Equity curve chart
    - Drawdown chart
    - Monthly returns heatmap
    - Trade list with entry/exit prices
    - Performance attribution by phase/strategy
    - Export to JSON/CSV for further analysis.
  - `parameterOptimizer.ts`: (Optional) Runs grid search or Bayesian optimization over strategy parameters (e.g., RL agent learning rate, jitter parameters) to find the best combination on historical data.
  - `index.ts`: Main entry point that orchestrates the backtest.

**Integration & Configuration**:
- Create `config.backtest.json`:
  ```json
  {
    "dataPaths": {
      "binance": "/data/binance/2023-2025",
      "bybit": "/data/bybit/2023-2025",
      "ethereum": "/data/ethereum/archive",
      "solana": "/data/solana/archive"
    },
    "dateRange": {
      "start": "2024-01-01",
      "end": "2025-01-01"
    },
    "phases": {
      "phase1": { "enabled": true, "modelPath": "./models/imbalance.onnx" },
      "phase2": { "enabled": true, "llmModel": "mock" },
      "phase3": { "enabled": true, "sandwichThreshold": 0.8 },
      "phase4": { "enabled": false },
      "phase5": { "enabled": true },
      "phase6": { "enabled": true, "jitterMeanMs": 8 },
      "phase7": { "enabled": true },
      "phase8": { "enabled": false },
      "phase9": { "enabled": false },
      "phase10": { "enabled": false },
      "phase11": { "enabled": false },
      "post12": { "enabled": false }
    },
    "fees": {
      "binance": { "maker": 0.0002, "taker": 0.0004 },
      "bybit": { "maker": 0.0001, "taker": 0.0003 },
      "uniswap": { "fee": 0.003 }
    },
    "initialCapital": 100000,
    "riskFreeRate": 0.02,
    "outputFormat": "html"
  }
  ```
- Provide npm script: `npm run backtest -- --config config.backtest.json`.
- Integrate with NextJS dashboard: add a tab to view historical backtest results.

**Implementation Notes**:
- Use `node-parquet` or `csv-parse` for data loading.
- For performance, simulate in batches (e.g., process 1 second of data at a time) rather than tick‑by‑tick if data density is high.
- The simulation engine must be deterministic (same input always produces same output) for reliable optimization.
- For parameter optimization, integrate with `optuna` (via Python bridge) or a simple TypeScript grid search.
- All external calls (to models, etc.) should be mocked or use pre‑computed outputs for speed.

**Testing & Quality**:
- Achieve 100% Jest coverage for all TypeScript files.
- Test with small synthetic datasets to verify PnL calculation.
- Ensure that enabling/disabling phases works correctly.
- Validate that fees and slippage are applied accurately.

Proceed with implementation.
"
