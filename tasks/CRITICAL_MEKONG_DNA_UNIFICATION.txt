/plan:hard "
MISSION: Refactor and Unify Mekong-CLI DNA (OpenClaw + ClaudeKit + Binh Phap).
Currently, the orchestration logic (105-agent swarm, mission-dispatcher, brain-process-manager, etc.) is scattered and hardcoded inside `apps/openclaw-worker/lib/`. 
This is WRONG. When `mekong-cli` is published, developers need access to these tools natively via the CLI, not locked inside an internal app.
At the same time, this repo is a mess (61+ folders, 96+ files at root).

Your Goal: Clean up the repository and extract the core orchestration/DNA logic into a unified, reusable `mekong-cli` package structure.
IMPORTANT: You must strip away any specific internal project traces (e.g., 'Tôm Hùm', 'Sophia', 'WellNexus') so the public CLI is clean and professional.

**Phase 1: Architecture Restructuring**
- Create or update a core package (e.g., `packages/vibe/` or a new `packages/orchestrator/`).
- Move the core logic from `apps/openclaw-worker/lib/` (e.g., mission dispatcher, swarm intelligence, safety guard, M1 cooling daemon) into this package.
- Clean up the `mekong-cli` root directory by moving miscellaneous scripts, logs, and temp files into `.archive/`, `.logs/`, or appropriate hidden folders.

**Phase 2: Genericizing the DNA (Binh Phap + ClaudeKit)**
- Read `apps/openclaw-worker/BINH_PHAP_MASTER.md` to understand the DNA structure (Ngũ Đức, Cửu Địa, Phong Lâm Hỏa Sơn).
- Implement this DNA natively into the `mekong-cli` commands. When a user runs a command, it should leverage the 13 ClaudeKit agents + 43 skills mapped to the Binh Phap strategies, entirely independent of `openclaw-worker`.
- Ensure all references to historical private project names ('tôm hùm', 'com anh duong') in the moved logic are generalized to "Swarm/Worker/Agent".

**Phase 3: CLI Command Integration**
- Expose the unified swarm logic through the `mekong-cli` binary. 
- A developer should be able to run something like `npx mekong-cli swarm start` to boot up the 105-agent CTO orchestration.
- The `openclaw-worker` app should be refactored to simply IMPORT and consume this new package logic, acting as just one implementation rather than the owner of the logic.

Execute this with extreme prejudice against messy code.
"
