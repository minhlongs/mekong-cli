/plan:hard "
MISSION: Implement a Performance Profiling & Auto-Tuning module for the 12-phase AGI trading system. The module must profile the entire stack (network I/O, CPU, memory, event loop lag) during simulated trading, identify bottlenecks, and automatically adjust configuration parameters (e.g., batch sizes, jitter settings, thread pool sizes) to optimize latency and throughput. All code in TypeScript with 100% Jest test coverage (mocking profiling tools). Provide a configuration file 'config.tuning.json'.

**MODULE: Performance Optimizer**
- File path: `apps/algo-trader/src/performance/optimizer/`
- Subcomponents:
  - `profiler.ts`: Uses Node.js built-in `perf_hooks`, `clinic` (if available), and `process.cpuUsage` to collect:
    - Event loop lag
    - Garbage collection stats
    - Function-level execution time (via async_hooks or manual instrumentation)
    - Network latency to mock exchanges
    - Order execution throughput (orders/sec)
  - `bottleneckDetector.ts`: Analyzes profiling data to identify top 5 bottlenecks (e.g., slow JSON parsing, synchronous crypto ops, excessive context switching). Outputs a ranked list with estimated impact.
  - `configMutator.ts`: For each bottleneck, suggests configuration changes:
    - Increase/dec rease batch sizes
    - Adjust jitter parameters (Phase 6)
    - Tune RL agent update frequency (Phase 7)
    - Modify proxy rotation interval (Phase 6)
    - Enable/disable certain features based on cost/benefit
  - `autoTuner.ts`: Applies suggested changes in a controlled manner (A/B testing) and measures improvement. If improvement > threshold, makes change permanent.
  - `reportGenerator.ts`: Produces an HTML report with before/after metrics, bottlenecks found, and actions taken.
  - `index.ts`: Main entry point.
- Tests: Mock profiling data; verify detector logic and config changes.

**Integration & Configuration**:
- Create `config.tuning.json`:
  ```json
 {
    "profiling": {
      "durationSec": 300,
      "sampleIntervalMs": 100,
      "includeClinic": false
    },
    "autoTune": {
      "enabled": true,
      "improvementThreshold": 0.1,
      "maxIterations": 5
    },
    "outputReport": "tuning_report.html"
  }
  ```
Provide npm script: npm run tune.

Implementation Notes:

Use clinic.js if installed globally, otherwise fallback to built-in metrics.

For network latency measurement, use http.get with timestamps.

Ensure auto-tuner respects min/max bounds for all parameters.

The module should not change production config without explicit approval (dry-run mode).

Proceed with implementation.
"
