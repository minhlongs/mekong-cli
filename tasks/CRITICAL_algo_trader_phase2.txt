/plan:hard "
MISSION: Implement Phase 2 of the AGI Trading System evolution. Create three new modules under 'apps/algo-trader/src/arbitrage/phase2/' that realize the following elite concepts:

1.  **Zero-Shot Strategy Synthesizer**:
    - Integrate a local LLM (Llama-3 8B quantized) to ingest real-time Twitter/Telegram streams (use mock streams for testing, with a real API integration planned). 
    - Build a pipeline: fetch messages → sentiment analysis → topic extraction → strategy rule generator → secure TypeScript code compilation.
    - The generated rules must be hot‑swappable without restarting the engine; use Node.js 'vm' module or a dynamic import with file watching.
    - Include a self‑validation step: before deployment, backtest the new rule on the last 24h of market data (sliding window) and only activate if Sharpe ratio > 1.5.
    - Write all code in 'phase2/zeroShotSynthesizer/' with separate files for llmClient.ts, ruleGenerator.ts, hotDeployer.ts, and index.ts exposing a start() method.
    - Create comprehensive Jest tests mocking the LLM and exchange data.

2.  **Decentralized Cross‑Chain Flash Loans**:
    - Extend the existing GNN graph router to include on‑chain DEX nodes (Uniswap V3 on Ethereum, Raydium on Solana) and bridge edges (Wormhole, Axelar).
    - Implement flash loan integration: support Aave V3 flash loans (Ethereum) and Solana native flash loans (via port finance). Use ethers.js and web3.js accordingly.
    - Build a smart‑order‑router that finds profitable paths involving both CEX and DEX steps, calculates expected profit after gas and bridge fees, and executes atomically.
    - For testing, use local forks (Hardhat, Anvil) and mock CEX orderbook data.
    - Place code in 'phase2/crossChainFlashLoans/' with subfolders: /dex, /bridges, /flashLoan, /router. Include full test coverage.

3.  **Adversarial Market Making (Spoofing Detection)**:
    - Train (or download) an ONNX model for spoofing/iceberg detection; the module must load it via ONNX Runtime Node.
    - Implement a real‑time detector that subscribes to orderbook deltas from the main engine and outputs a confidence score for ongoing manipulation.
    - When score > 0.8, the arbitrage engine should: a) avoid placing orders behind a suspected spoof, b) optionally submit fade orders (opposite side) if confidence is very high.
    - Provide a hook to override the main arbitrage logic; integrate with the existing strategy manager.
    - Code location: 'phase2/adversarialMM/' with detector.ts, modelLoader.ts, and integration tests.

**Integration & Configuration**:
- Update the main arbitrage engine (likely 'arbitrageOrchestrator.ts') to conditionally activate these modules based on a new config file 'config.phase2.json' placed at the project root.
- The config must allow enabling/disabling each module independently and supply parameters (e.g., LLM model path, RPC endpoints, flash loan providers).
- Expose real‑time metrics from each module via the existing WebSocket feed (add new message types) and create corresponding UI panels in the NextJS dashboard (simple placeholder panels with current status are acceptable for now, but the data flow must be there).
- All modules must include robust error handling and fallback to Phase 1 behavior if they fail or are disabled.
- Write a new 'README-Phase2.md' documenting the architecture, configuration, and testing instructions.

**Testing & Quality**:
- Achieve 100% test coverage for all new TypeScript files using Jest. Mock external dependencies (LLM, blockchain RPC, exchange APIs) thoroughly.
- Include integration tests that simulate end‑to‑end scenarios (e.g., a Twitter post triggering a strategy, a flash loan arbitrage completing on a forked chain).
- Follow the existing project's linting (ESLint) and formatting (Prettier) rules.

**Execution Notes**:
- The agent is expected to write all necessary code, configuration files, and documentation.
- Use existing project patterns for logging, error handling, and dependency injection where appropriate.
- Ensure the system remains production‑grade: all external calls must have timeouts, retries, and circuit breakers.
- The final commit should include a summary of changes and any new environment variables required.

Proceed with implementation.
"
