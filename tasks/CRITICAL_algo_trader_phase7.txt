/plan:hard "
MISSION: Implement Phase 7 – Autonomous Alpha Nexus (AAN) – to elevate our algo-trader to the next level of predictive and autonomous trading. Create three advanced modules under 'apps/algo-trader/src/arbitrage/phase7_aan/' as detailed below. All code must be in TypeScript, with 100% Jest test coverage (using mocks for external services). Integrate with the existing core strategy engine via middleware hooks and configuration flags. Provide a configuration file 'config.phase7.json' at the project root.

**MODULE 1: Predictive Liquidity Engine (PLE)**
- File path: `phase7_aan/predictiveLiquidity/`
- Subcomponents:
  - `featureExtractor.ts`: Converts raw orderbook deltas (bids/asks) and recent trades into a fixed‑size feature vector: volume imbalance at top levels, order book slope, micro‑price, trade flow intensity. Uses rolling windows.
  - `orderbookImbalanceModel.ts`: Loads a pre‑trained Transformer model (ONNX format) that takes the feature sequence and outputs predicted imbalance (buy/sell pressure) for the next 10–100 ms. For testing, a mock model returns random probabilities.
  - `liquidityScoreCalculator.ts`: Combines model output with current spread and depth to produce a liquidity score (0–1) per symbol.
  - `alphaSignalGenerator.ts`: When predicted imbalance exceeds a threshold (e.g., >0.7) and liquidity score is safe, emits a signal to place a pre‑emptive limit order on the expected side.
  - `index.ts`: Main loop that runs every 5 ms, updates features, runs inference, and emits signals via an event bus.
- Tests: Mock orderbook updates, verify feature extraction, model inference, and signal emission. 100% coverage.

**MODULE 2: Multi-Agent RL Spread Optimizer (MARSO)**
- File path: `phase7_aan/rlSpreadOptimizer/`
- Subcomponents:
  - `marketEnv.ts`: Implements a Gym‑like environment for each trading pair. State includes: current spread, order book depth at top levels, 1‑minute volatility, inventory ratio. Action space: continuous adjustments to spread (±x%) and order size multiplier. Reward: realized PnL minus inventory risk penalty.
  - `rlAgent.ts`: Uses TensorFlow.js to implement a PPO agent (or DQN for discrete actions). For testing, a simple rule‑based agent is provided. Training can be done offline using historical data stored in Parquet files.
  - `policyExecutor.ts`: Loads the latest trained policy (saved as a JSON model) and applies actions in real time, updating limit order parameters on the exchange connectors.
  - `rewardCalculator.ts`: Computes reward after each fill or after a fixed interval, considering filled volume, slippage, and inventory.
  - `trainingOrchestrator.ts`: Runs daily training jobs on a separate thread/process, using historical market data to improve the policy. Deploys new models atomically.
  - `index.ts`: Starts the environment‑agent loop for each configured pair.
- Tests: Mock environment transitions, verify agent action application, reward calculation, and model loading. 100% coverage.

**MODULE 3: Cross-Chain Triangular Arbitrage with MEV Protection (CCTAMP)**
- File path: `phase7_aan/crossChainArbitrage/`
- Subcomponents:
  - `chainGraphBuilder.ts`: Maintains a dynamic directed graph where nodes are exchanges (CEX + DEX across chains) and edges represent tradable pairs with current prices, liquidity, and fees. Updates from WebSocket streams and on‑chain RPC calls.
  - `pathFinder.ts`: Runs Bellman‑Ford at 100ms intervals to detect negative‑weight cycles (profitable triangular paths) of length ≥3, considering gas costs, bridge fees, and slippage. Outputs a ranked list of opportunities.
  - `flashLoanManager.ts`: Integrates with Aave V3 (Ethereum), dYdX (StarkNet), and Solana flash loan providers. For each opportunity, it calculates required capital and checks availability.
  - `mevProtector.ts`: For Ethereum‑based legs, constructs a Flashbots bundle that includes the flash loan, DEX swaps, and repayment, and submits it via `@flashbots/ethers-provider-bundle`. For Solana, uses Jito’s bundle service. Ensures transactions are private and ordered.
  - `atomicExecutor.ts`: Coordinates the entire multi‑chain trade: acquires flash loan, executes DEX swaps (via web3 calls) and CEX trades (via exchange APIs) in a tightly timed sequence, then repays loan. Handles failures and reverts.
  - `index.ts`: Continuously monitors for opportunities and triggers execution when profitability exceeds a dynamic threshold.
- Tests: Mock blockchain RPCs, exchange APIs, and bundle relays. Simulate profitable cycles and verify execution logic. 100% coverage.

**Integration & Configuration**:
- Create `config.phase7.json` with the following schema:
  ```json
  {
    "predictiveLiquidity": {
      "enabled": false,
      "modelPath": "./models/imbalance.onnx",
      "threshold": 0.7,
      "updateIntervalMs": 5
    },
    "rlSpreadOptimizer": {
      "enabled": false,
      "pairs": ["BTC/USDT", "ETH/USDT"],
      "modelPath": "./models/ppo_btc.json",
      "trainingIntervalHours": 24
    },
    "crossChainArbitrage": {
      "enabled": false,
      "chains": ["ethereum", "solana", "bsc"],
      "minProfitUsd": 50,
      "flashLoanProviders": ["aave", "jito"],
      "mevRelay": "flashbots"
    }
  }
  ```
- Modify the main orchestrator (`arbitrageOrchestrator.ts`) to conditionally start each Phase 7 module based on the config.
- Expose metrics via WebSocket:
  - PLE: current imbalance predictions, signal count.
  - MARSO: current spreads per pair, average reward.
  - CCTAMP: number of opportunities found, executed trades, MEV bundle status.
- Add dashboard panels in NextJS to display these metrics (placeholders accepted).

**Testing & Quality**:
- Achieve 100% Jest coverage for all TypeScript files. Use `jest.mock` for external libraries (TensorFlow.js, ONNX runtime, Web3, exchange APIs).
- For modules interacting with blockchain, use `hardhat` forks or `ganache` for integration tests (but mocks are sufficient for unit tests).
- Ensure backward compatibility: if any module fails or is disabled, the system continues with previous phases.

**Implementation Notes**:
- For ONNX runtime in Node.js, use `onnxruntime-node`. For TensorFlow.js, use `@tensorflow/tfjs-node`.
- For Ethereum interactions, use `ethers.js` v6; for Solana, use `@solana/web3.js`.
- Flashloan integration: Aave V3 has a simple `flashLoan` method; dYdX requires a different interface. Provide adapters.
- MEV protection: Flashbots requires signing bundles with a private key; Jito uses gRPC. Use mock implementations for tests.
- All modules must log extensively and support dry‑run mode (no real orders).

Proceed with implementation.
"
