/plan:hard "
MISSION: Implement Phase 4 of the AGI Trading System – the Omniscience Paradigm. Create three advanced modules under 'apps/algo-trader/src/arbitrage/phase4/' as described below. **IMPORTANT: All modules must operate in SIMULATION MODE only, with no access to real funds or mainnet private keys.** Use mock APIs, local testnet forks, and in‑memory simulators. Achieve 100% Jest test coverage and integrate with the NextJS dashboard via new WebSocket metrics.

**MODULE 1: Dark AI Polymarket Sentinel**
- File path: `phase4/polymarketSentinel/`
- Subcomponents:
  - `newsIngestor.ts`: Connect to mock news API (simulate Reuters/Bloomberg feeds) – use a local JSON file with timestamped headlines and sentiment pre‑labeled. For real integration, design an interface that could later connect to a news API, but for tests, use mock data.
  - `predictionMarketFetcher.ts`: Fetch mock Polymarket and Kalshi contract data (prices, volume). Use static JSON fixtures or a simple mock server.
  - `nlpProcessor.ts`: Load a lightweight sentiment model (e.g., DistilBERT via ONNX) to score headlines. For tests, use a rule‑based fallback.
  - `signalFusionEngine.ts`: Combine NLP sentiment and market probabilities into a composite macro drift signal. Use a Kalman filter or weighted average.
  - `assetCorrelator.ts`: Maintain a dynamic regression model linking macro drift to crypto asset returns. For simulation, use pre‑computed correlation matrix from historical data.
  - `executor.ts`: When signal exceeds threshold, generate simulated trade orders (no real execution) and log them. Emit metrics (signal strength, simulated PnL).
  - `index.ts`: Main loop running every 5 seconds.
- Tests: 100% coverage with mocks for all external calls.

**MODULE 2: On‑Chain Sybil Sentiment Fabrication (Simulation Only)**
- File path: `phase4/sybilMirage/`
- Subcomponents:
  - `walletGenerator.ts`: Create deterministic mock Ethereum/Solana addresses using `ethers.Wallet.createRandom()` with a fixed seed (for reproducibility). Store private keys in memory only.
  - `txOrchestrator.ts`: Simulate sending micro‑transactions on a local Hardhat fork or Ganache. Use `ethers` to interact with a mock ERC‑20 contract on the fork. For Solana, use `@solana/web3.js` with a local validator.
  - `patternDetector.ts`: Monitor the simulated mempool and on‑chain activity of other mock bots (implement a simple bot that reacts to transaction patterns). Detect when external simulated bots start buying based on the fabricated accumulation.
  - `dumpSimulator.ts`: When triggered, simulate a large sell order from a main wallet and record the price impact.
  - `index.ts`: Orchestrate the entire simulation, with configurable parameters (number of wallets, transaction intervals, etc.).
- Tests: All interactions are with local forks; use `jest` to verify transaction sequences and state changes.

**MODULE 3: CEX VIP‑Tier Orderbook Spoofing (Simulation)**
- File path: `phase4/shadowLayering/`
- Subcomponents:
  - `exchangeSimulator.ts`: Build an in‑memory orderbook that replicates Binance’s matching engine, including iceberg orders, limit/market orders, and liquidations. Use event‑driven architecture.
  - `shadowLayeringStrategy.ts`: Implements the spoofing logic: based on current simulated market conditions (volatility, open interest), decide to place large visible orders at key levels (e.g., just above resistance). Monitor price movement; cancel orders when price approaches. Log all actions.
  - `impactAnalyzer.ts`: Measure the effect on simulated liquidations, price movement, and other bots’ behavior.
  - `defensiveDetector.ts`: (Optional) Implement a detector that identifies similar spoofing patterns in real market data (for future use). For now, run it against the simulator to verify detection.
  - `index.ts`: Run the simulation loop.
- Tests: Validate orderbook consistency, strategy behavior, and impact measurement.

**Integration & Configuration**:
- Create `config.phase4.json` with sections:
  ```json
  {
    "polymarketSentinel": {
      "enabled": false,
      "newsSource": "mock",
      "pollIntervalMs": 5000,
      "signalThreshold": 0.7
    },
    "sybilMirage": {
      "enabled": false,
      "numWallets": 1000,
      "txIntervalMs": 100,
      "dumpThreshold": 0.8
    },
    "shadowLayering": {
      "enabled": false,
      "simulatedLatencyMs": 10,
      "icebergSizes": [10, 50, 100]
    }
  }
  ```
- Update the main orchestrator (`arbitrageOrchestrator.ts`) to conditionally instantiate and run each Phase 4 module based on the config.
- Expose metrics via WebSocket: for Polymarket Sentinel: current macro drift, simulated trades; for Sybil Mirage: number of active wallets, detected pattern triggers; for Shadow Layering: orderbook snapshots, simulated liquidations.
- Add placeholder UI panels in the NextJS dashboard to display these metrics.

**Testing & Quality**:
- Achieve 100% Jest coverage for all TypeScript files. Use `jest.mock` for any external API calls (but note that most interactions are internal simulations).
- For modules interacting with local forks, use `@nomiclabs/hardhat-ethers` and ensure tests clean up after themselves.
- All simulation modules must have zero ability to connect to real mainnet or exchange APIs. Use environment variables to enforce simulation mode (e.g., `NODE_ENV=simulation`).

**Implementation Notes**:
- For `polymarketSentinel`, the NLP model can be a simple `transformers.js` pipeline (e.g., `pipeline('sentiment-analysis')`). For tests, use a mock.
- For `sybilMirage`, use Hardhat to fork a mainnet state (or use a blank local chain). Provide scripts to set up a mock ERC‑20 token.
- For `shadowLayering`, the exchange simulator must be a faithful reproduction of central limit order book mechanics. Consider using `node-binance-api` as a reference but build your own lightweight version.
- All modules must log extensively and support dry‑run mode (no simulated trades if disabled).
- Document all simulation parameters and how to run the modules in a safe, isolated environment.

Proceed with implementation, ensuring strict adherence to simulation‑only constraints.
"
