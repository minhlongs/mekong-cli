/plan:hard "
MISSION: Implement the Post‑Phase 12 Operational Framework to transform our 12‑phase AGI trading system into a production‑ready autonomous fund. Create three core modules under 'apps/algo-trader/src/post12_operations/' as detailed below. All code must be in TypeScript, with 100% Jest test coverage (using mocks for external services). Integrate with the existing core engine via configuration flags and provide a master orchestration script. Provide a configuration file 'config.post12.json' at the project root.

**MODULE 1: System Integration & Chaos Testing Environment**
- File path: `post12_operations/chaosTesting/`
- Subcomponents:
  - `stagingOrchestrator.ts`: Spins up a complete staging environment using Docker Compose (simulated in code). It launches:
    - Mock exchange servers (Binance, Bybit, Uniswap) with configurable latency, error rates, and orderbook behavior.
    - A local Hardhat fork for on‑chain interactions (Ethereum, Solana validator in simulator mode).
    - All 12 phase modules, each running in simulation mode (no real orders).
  - `chaosScheduler.ts`: Randomly injects failures at configurable intervals:
    - Kill a random module process.
    - Introduce network latency (50‑500ms) between components.
    - Drop a percentage of messages.
    - Simulate exchange downtime or rate‑limiting.
  - `recoveryValidator.ts`: After each chaos event, verifies that the system:
    - Detects the failure (via health checks).
    - Executes the appropriate fallback (e.g., Phase 5 circuit breaker, Phase 8 swarm reallocation).
    - Returns to normal operation within a time limit.
  - `reportGenerator.ts`: Produces a detailed HTML report of chaos test results, including timelines, failures, and recovery metrics.
  - `index.ts`: Main entry point that runs the chaos suite for a specified duration.
- Tests: Mock chaos events and verify that recovery logic is triggered. 100% coverage.

**MODULE 2: Canary Deployment Pipeline**
- File path: `post12_operations/canaryDeploy/`
- Subcomponents:
  - `configManager.ts`: Reads `config.post12.json` and manages multiple configuration profiles (e.g., `dev`, `staging`, `canary-live`). Supports gradual rollout of new phase versions.
  - `deployer.ts`: Implements a blue‑green deployment strategy:
    - Starts a new instance of the system with the canary config.
    - Routes a small percentage of real capital (e.g., 1%) to the canary.
    - Monitors key metrics: Sharpe ratio, slippage, API error rate, anti‑bot flags.
  - `metricsComparator.ts`: Compares canary metrics with the current production instance using statistical tests (t‑test, Bayesian). If canary underperforms or triggers flags, triggers rollback.
  - `autoRollback.ts`: If metrics degrade beyond thresholds, automatically switches traffic back to the production instance and logs the incident.
  - `alertManager.ts`: Sends alerts via Telegram/Email/Slack when canary fails or when human intervention is needed.
  - `index.ts`: Orchestrates the canary lifecycle.
- Tests: Mock metrics and verify rollback logic. 100% coverage.

**MODULE 3: Autonomous Operations & Self‑Evolution**
- File path: `post12_operations/autonomousOps/`
- Subcomponents:
  - `incidentAnalyzer.ts`: Subscribes to all system logs and metrics. Uses a local LLM (via Ollama or llama.cpp) to classify incidents into categories (network, exchange, strategy, hardware). Suggests root cause and potential fixes.
  - `evolutionTrigger.ts`: Monitors strategy performance (Sharpe, drawdown) across all 12 phases. When a strategy underperforms for a configurable period, it triggers the Genetic Synthesizer (Phase 5) to spawn new variants.
  - `autoTuner.ts`: Uses Bayesian optimization to adjust hyperparameters of active modules (e.g., RL agent learning rate, jitter parameters, proxy rotation interval) based on recent performance.
  - `governanceProposer.ts`: If a new strategy outperforms the old one by a significant margin, it automatically creates an on‑chain proposal (Phase 8 DAO) to replace it. For simulation, just logs the proposal.
  - `humanFeedbackLoop.ts`: Exposes a simple REST API for a human operator to approve/reject proposals, override decisions, or inject guidance. Integrates with the BCI module (Phase 11) if enabled.
  - `index.ts`: Main loop that runs the analysis and evolution cycle.
- Tests: Mock LLM responses and performance data; verify trigger logic. 100% coverage.

**Integration & Configuration**:
- Create `config.post12.json` with the following schema:
  ```json
  {
    "chaosTesting": {
      "enabled": false,
      "durationHours": 24,
      "chaosEventsPerHour": 5,
      "mockExchanges": ["binance", "bybit", "uniswap"]
    },
    "canaryDeploy": {
      "enabled": false,
      "canaryPercent": 1,
      "evaluationPeriodMinutes": 60,
      "rollbackThresholds": {
        "sharpeRatioDrop": 0.5,
        "errorRateIncrease": 0.1
      },
      "alertChannels": ["telegram", "slack"]
    },
    "autonomousOps": {
      "enabled": false,
      "llmModel": "llama3:8b",
      "evolutionCheckIntervalHours": 24,
      "autoTuneIntervalHours": 6,
      "humanFeedbackApi": true
    }
  }
  ```
- Create a master orchestrator `runPost12.ts` that can start any combination of the three modules based on the config.
- Expose metrics via WebSocket: chaos test status, canary metrics comparison, evolution proposals.
- Add dashboard panels in NextJS to display these operational metrics.

**Testing & Quality**:
- Achieve 100% Jest coverage for all TypeScript files.
- For chaos testing, use `jest` fake timers to simulate chaos events and verify recovery.
- For canary deploy, mock the production and canary instances to compare metrics.
- For autonomous ops, mock LLM responses and use a simple rule‑based fallback for tests.
- Ensure all modules can be disabled individually and have no impact on core trading when off.

**Implementation Notes**:
- For LLM integration, use `ollama` or `llama.cpp` via a REST API. Provide a mock LLM for tests.
- For Docker simulation, use `dockerode` or just mock the container lifecycle.
- The chaos testing module should not actually run real Docker containers in unit tests; mock the calls.
- All modules must log extensively and support dry‑run mode.

Proceed with implementation.
"
