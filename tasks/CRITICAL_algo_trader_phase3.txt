/plan:hard "
MISSION: Implement Phase 3 of the AGI Trading System, transforming the bot into an apex MEV extractor and predatory market maker. Create three new elite modules under 'apps/algo-trader/src/arbitrage/phase3/' as detailed below. All code must be in TypeScript, with 100% Jest test coverage (using mocks for external dependencies). Integrate with the existing NextJS dashboard via new WebSocket metrics and UI panels. Provide a configuration file 'config.phase3.json' at the project root to enable/disable each module independently.

**MODULE 1: Cross-CEX/DEX MEV Sandwich Architecture**
- File path: `phase3/mevSandwich/`
- Subcomponents:
  - `mempoolMonitor.ts`: Connect to Ethereum (via WebSocket to a node + Flashbots Mempool Dumpster) and Solana (via Jito mempool WebSocket) to receive pending transactions. Use mock streams for testing.
  - `sandwichDetector.ts`: For each relevant pending swap (e.g., large Uniswap V3 trade), simulate its impact using a local fork (via Hardhat mainnet fork or Alchemy simulate). Compute optimal sandwich sizes and prices, factoring in gas, slippage, and CEX hedge (using Binance/Bybit orderbook data). Output a `SandwichOpportunity` object.
  - `bundleBuilder.ts`: Construct Ethereum bundles (Flashbots) or Solana bundles (Jito) that place the preemptive buy, the victim transaction, and the post-sell. Include error handling and revert protection.
  - `executor.ts`: Submit bundles via Flashbots relay (ethers.js) or Jito (grpc-web) with retry logic and timeout. Track inclusion status.
  - `index.ts`: Exports a `start()` method that continuously runs the detection-execution loop.
- Tests: Mock mempool data, simulate bundle submissions, verify profit calculation.

**MODULE 2: Quantum-Inspired Portfolio Rebalancer**
- File path: `phase3/portfolioRebalancer/`
- Subcomponents:
  - `exposureAggregator.ts`: Fetches real-time balances and positions from all configured exchanges (Binance, Bybit, OKX, etc.) and DeFi protocols (Aave, Compound). Normalize into a unified asset-exposure matrix.
  - `riskCalculator.ts`: Compute Delta, Gamma, Vega per asset across all venues using options greeks (if options are held) or simple delta for perps/spot.
  - `optimizerCore.ts`: Implement a fast stochastic optimizer (simulated annealing or gradient descent) that solves for target allocations to minimize risk while maximizing yield (funding rates, lending APY). Use a cost function that includes transfer fees and slippage. For performance, consider a WebAssembly module or a Python microservice (with FastAPI) – but prefer pure TypeScript with numeric.js or similar.
  - `rebalancingExecutor.ts`: Generate a list of minimal-impact trades to move from current to target allocation. Execute them via exchange APIs with smart order routing.
  - `index.ts`: Main loop running every 500ms.
- Tests: Mock exchange balances, test optimizer against known scenarios, verify trade generation.

**MODULE 3: Predatory Liquidity Trapping (Pump-and-Dump Exploitation)**
- File path: `phase3/predatoryLiquidity/`
- Subcomponents:
  - `pumpDetector.ts`: Consume social media streams (from Phase 2) and on-chain whale alerts (e.g., large transfers to Binance). Use a time-series transformer model (ONNX) to output dump probability in real time. Model must be trained offline and loaded via ONNX Runtime.
  - `liquidityTrapper.ts`: When pump probability > threshold, switch strategy: place maker limit orders on the ask side (to capture fees and potential short funding) and simultaneously open a small short position (if perps exist) to hedge. Continuously adjust orders based on price movement.
  - `dumpExecutor.ts`: Monitor dump probability; when it exceeds a critical threshold (e.g., 0.9), instantly cancel all maker orders and execute a market short (or flip existing long to short) to profit from the crash. Use aggressive IOC orders.
  - `index.ts`: Orchestrates the detector and trader, with configurable thresholds.
- Tests: Mock pump events, verify order placement and cancellation logic, simulate dump scenarios.

**Integration & Configuration**:
- Create `config.phase3.json` with sections:
  ```json
  {
    "mevSandwich": { "enabled": false, "ethereumRpc": "wss://...", "flashbotsRelay": "https://...", "solanaJitoUrl": "...", "minProfitUsd": 10 },
    "portfolioRebalancer": { "enabled": false, "intervalMs": 500, "exchanges": ["binance","bybit"], "maxSlippageBps": 5 },
    "predatoryLiquidity": { "enabled": false, "pumpThreshold": 0.7, "dumpThreshold": 0.9, "makerSpreadBps": 2 }
  }
  ```
- Update the main arbitrage orchestrator (`arbitrageOrchestrator.ts`) to conditionally instantiate and run each Phase 3 module based on the config.
- Expose metrics via WebSocket: for MEV module, include opportunities found/bundles submitted/success rate; for rebalancer, include current risk exposures and rebalancing actions; for predatory, include pump probability and active maker positions.
- Add placeholder UI panels in the NextJS dashboard (new tabs or cards) to display these metrics.

**Testing & Quality**:
- Achieve 100% Jest coverage for all TypeScript files. Use `jest.mock` extensively for external APIs (Web3, exchange REST, WebSocket).
- Include integration tests that simulate end-to-end scenarios: e.g., a pending transaction triggers a sandwich bundle; a pump event leads to maker orders.
- Ensure all modules have graceful degradation: if a component fails, log error and disable only that module, continuing with others.
- Follow existing linting (ESLint) and formatting (Prettier) rules.

**Implementation Notes**:
- For MEV module, use `ethers.js` v6 for Ethereum interactions and `@solana/web3.js` for Solana. For Flashbots, use `@flashbots/ethers-provider-bundle`. For Jito, use their gRPC or REST API.
- For rebalancer, consider using `numjs` or `ml-matrix` for matrix operations.
- For predatory module, the ONNX model can be a simple LSTM trained offline; provide a script to generate a mock model for testing.
- Document all new environment variables required (e.g., `ETH_RPC_URL`, `FLASHBOTS_RELAY`, `JITO_AUTH_TOKEN`) in a `.env.example` update.

Proceed with full implementation.
"
