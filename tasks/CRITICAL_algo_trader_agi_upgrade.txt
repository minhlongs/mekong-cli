/plan:hard "
MISSION: Upgrade the existing 'algo-trader' project from V2 reactive HFT to a Level 5 Autonomous AGI Trading System. Implement three core AGI concepts:

1.  Predictive Orderbook ML – Integrate a Temporal Fusion Transformer (TFT) that ingests real-time L2 orderbook deltas, trade prints, and optional news sentiment to forecast the orderbook imbalance and price movement for the next 10–50 ms. The model must be served via a lightweight ONNX Runtime or TensorFlow.js pipeline within the Node.js process, with fallback to a simple moving average if the model is unavailable.

2.  Graph-Based Multi-Exchange Arbitrage – Replace the current 2-leg arbitrage logic with a dynamic graph representation of all connected exchanges. Implement a GNN-based pathfinder that continuously evaluates all possible triangular and cross-exchange routes (3+ legs) for expected profitability, factoring in real-time latency, depth, and exchange fees. The system must atomically execute multi-leg trades using a smart-order-router that splits orders across venues to minimize slippage.

3.  Adaptive Latency Compensation via RL – Develop a lightweight online reinforcement learning agent (e.g., using a small neural network with experience replay) that learns the joint distribution of network latency and exchange processing delays. The agent must dynamically adjust order placement timing, cancellation strategies, and optionally select the optimal regional endpoint to minimize execution risk. Use a simple reward function based on filled vs. expected slippage.

Implementation Requirements:
- All new code must reside in 'apps/algo-trader/src/arbitrage' with proper subdirectories: /ml, /graph, /rl, /strategies.
- Write comprehensive unit and integration tests achieving 100% coverage for all new modules. Use Jest and mock exchange data.
- Integrate the new AGI strategies with the existing NextJS dashboard by exposing real-time metrics (predicted imbalance, active graph routes, RL latency adjustments) via the existing WebSocket feed and adding new panels to the UI.
- Ensure backward compatibility: the system must fall back to the V2 reactive arbitrage if any AGI component fails or if confidence scores are low.
- Provide a configuration file (config.agi.json) to enable/disable each AGI feature independently.
- Document the architecture in a new README-AGI.md.

The agent must create all necessary TypeScript files, model loading utilities, and update the main arbitrage engine to orchestrate the new components. All code must follow the existing project's linting and formatting standards.

Execute this plan with production-grade robustness: include error handling, graceful degradation, and performance monitoring hooks.
"
