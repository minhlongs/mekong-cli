/plan:hard "
MISSION: Implement Phase 5 – God Mode – transforming the algo-trader into a fully autonomous multi-agent quant fund. Create three core modules under 'apps/algo-trader/src/arbitrage/phase5_god_mode/' as detailed below. All code must be in TypeScript (except the Rust/WASM module) with 100% Jest test coverage for TS parts, and integration tests for the Rust module using wasm-pack and Jest. Integrate with the NextJS dashboard via new WebSocket metrics and UI panels. Provide a configuration file 'config.phase5.json' at the project root to enable/disable modules and set parameters.

**MODULE 1: Decentralized Multi-Agent Fund Manager (DAO Brain)**
- File path: `phase5_god_mode/daoBrain/`
- Subcomponents:
  - `agents/` – Four agent implementations:
    - `riskManager.ts`: Consumes risk metrics (VaR, max drawdown, concentration) and outputs a proposed allocation cap per strategy.
    - `alphaSeeker.ts`: Consumes strategy performance (Sharpe, win rate, profit factor) and proposes allocation increases to top strategies.
    - `executioner.ts`: Monitors slippage, fill rates, and exchange health; proposes reducing allocation to venues with high latency.
    - `macroEconomist.ts`: Fetches macro data (interest rates, inflation, VIX) and proposes risk‑off or risk‑on adjustments.
  - `consensusEngine.ts`: Implements a weighted voting mechanism. Each agent submits a vector of allocation adjustments (±%). The engine computes the weighted average, ensuring no single agent exceeds 5% influence. Outputs final allocation changes.
  - `stakeManager.ts`: Updates each agent's stake based on the accuracy of their past proposals (e.g., if an agent's proposed allocation led to higher portfolio Sharpe, its stake increases).
  - `orchestrator.ts`: Runs the consensus loop every 4 hours (configurable), applies allocation changes to the strategy manager, and logs decisions.
  - `index.ts`: Exports a `start()` method.
- Tests: Mock agent inputs, verify voting logic, stake updates, and boundary conditions (agent trying to exceed 5%).

**MODULE 2: Meta-Strategy Genetic Synthesizer (Evolution Engine)**
- File path: `phase5_god_mode/evolutionEngine/`
- Subcomponents:
  - `genotype.ts`: Define the strategy genotype interface (e.g., `{ entryCondition: 'crossAbove', indicator: 'SMA', period: 20, asset: 'BTC/USDT', riskPercent: 1 }`). Use a flexible JSON schema.
  - `populationManager.ts`: Maintains a pool of genotypes in a simple JSON file or in‑memory DB. Provides methods to add, remove, and query strategies by fitness.
  - `geneticOperators.ts`: Implements selection (tournament), crossover (single‑point), mutation (random tweak to parameters), and fitness evaluation (calls backtesting).
  - `backtestEngine.ts`: Uses historical market data (from mock files) to simulate a strategy's performance. Returns Sharpe ratio, max drawdown, etc.
  - `codeGenerator.ts`: Takes a genotype and generates a valid TypeScript strategy file (e.g., `generatedStrategy_123.ts`) with proper exports, ready to be hot‑loaded by the main engine.
  - `evolutionScheduler.ts`: Runs the evolution loop every N minutes: select parents, create offspring, backtest, update population, and promote top 1% to live. Uses a simple RL controller to adjust mutation rate based on diversity.
  - `index.ts`: Main entry point.
- Tests: Mock backtest results, verify genetic operators, code generation, and promotion logic.

**MODULE 3: Capital Fortress & Doomsday Circuit Breaker**
- File path: `phase5_god_mode/capitalFortress/`
- Subcomponents:
  - `rust/` – Rust crate (to be compiled to WASM):
    - `src/lib.rs`: Defines the core monitoring and anomaly detection logic. Uses `reqwest` to fetch exchange health endpoints, on‑chain oracle prices (via RPC), and VIX data. Implements an Isolation Forest model (or loads an ONNX model) to compute collapse probability.
    - `Cargo.toml`: Dependencies: `wasm-bindgen`, `js-sys`, `serde`, `ort` (for ONNX runtime), `tokio` (if needed).
    - Build script to output `capital_fortress_bg.wasm` and `capital_fortress.js`.
  - `wasmBridge.ts`: Loads the WASM module and provides a clean TypeScript interface. Exposes `startMonitoring(callbacks)` and `getCollapseProbability()`.
  - `emergencyProtocol.ts`: Listens for a `'CRITICAL_COLLAPSE'` event from the WASM module. When triggered, it:
    - Pauses all trading strategies (via the main orchestrator).
    - Executes emergency withdrawal: uses pre‑configured multi‑sig addresses and signs transactions (in simulation mode, just logs).
    - Emits a global alert and shuts down the system (or hibernates).
  - `healthMonitor.ts`: (Optional) Fallback Node.js monitor if WASM fails.
  - `index.ts`: Initializes the WASM module, sets up event listeners, and starts monitoring.
- Tests: Use a mock WASM module that simulates crash probabilities. Test emergency trigger and shutdown sequence.

**Integration & Configuration**:
- Create `config.phase5.json`:
  ```json
  {
    "daoBrain": {
      "enabled": false,
      "votingIntervalHours": 4,
      "maxAgentInfluence": 0.05
    },
    "evolutionEngine": {
      "enabled": false,
      "populationSize": 1000,
      "evolutionIntervalMinutes": 60,
      "promotionRate": 0.01
    },
    "capitalFortress": {
      "enabled": false,
      "collapseThreshold": 0.99,
      "coldStorageAddresses": ["0x...", "0x..."],
      "emergencyWithdrawal": true
    }
  }
  ```
- Update `arbitrageOrchestrator.ts` to conditionally start each Phase 5 module based on config.
- Expose metrics via WebSocket:
  - DAO Brain: current allocations per agent, stake levels, last vote results.
  - Evolution Engine: population diversity, top 10 strategies, generation count.
  - Capital Fortress: collapse probability, last health check, emergency status.
- Add dashboard panels in NextJS to visualize these metrics (placeholders accepted, but data flow must be present).

**Testing & Quality**:
- Achieve 100% Jest coverage for all TypeScript files (excluding Rust code). Mock all external calls (exchange APIs, blockchain RPCs).
- For Rust/WASM, write integration tests using `wasm-pack test` and ensure the module can be loaded in Node.js.
- Use `jest` with `@wasm-tool/wasm-pack-plugin` to test the bridge.
- All modules must run in simulation mode by default, with no real funds at risk. Provide environment flags to enable dry‑run or live mode.

**Implementation Notes**:
- For the DAO Brain agents, use simple rule‑based logic initially; later they can be upgraded to LLM calls. Provide a mock agent that returns plausible allocation vectors.
- The genetic algorithm should store strategies in a local SQLite database (or JSON file) for persistence across restarts.
- The Rust module should be compiled to `wasm32-unknown-unknown` and included in the final bundle. Use `wasm-pack` to generate the Node.js bindings.
- Document all new environment variables (e.g., `RUST_BACKTRACE`, `EMERGENCY_COLD_KEY`) in a `.env.example`.
- Provide a script to pre‑train the anomaly detection model and export it to ONNX.

Proceed with full implementation, ensuring the system is now a true autonomous quant fund.
"
