/plan:hard "
MISSION: Upgrade the CTO Swarm Architecture from a static 3-role system to a dynamic 105-Agent 'Hands' Specialist Registry. 
The physical execution limit remains STRICTLY 3 CC CLI tmux panes (tom_hum:0.1, tom_hum:0.2, tom_hum:0.3). You MUST NOT spawn new tmux panes.
Instead, when the CTO dispatches a task to a pane, it must dynamically inject the persona, system prompt, and context of 1 out of 105 specialized agents based on the task intent.

**MODULE 1: The 105-Hands Registry**
- Refactor `apps/openclaw-worker/lib/hands-registry.js` (or use a new directory structure `lib/hands/`) to contain exactly 105 distinct specialist roles. 
- Example roles: FRONTEND_PERF_EXACTOR, POSTGRES_DBA, CLOUDFLARE_SECURITY_AUDITOR, SEO_COPYWRITER, SMART_CONTRACT_DEV, REACT_NATIVE_EXPERT, etc.
- Each role must have:
  - `name`: string
  - `systemPrompt`: highly specific instructions for that role.
  - `defaultCommand`: e.g., '/cook --fast', '/plan:hard', '/review'.
  - `keywords`: array of strings/regex used for matching.

**MODULE 2: Intent-to-Role Semantic Matcher**
- Refactor `apps/openclaw-worker/lib/mission-complexity-classifier.js` (or create `role-matcher.js`) to intelligently map the raw task text to the exact 1/105 role using keyword/regex scoring.
- If no specific match is found, fallback to a general role.

**MODULE 3: Dispatcher Injection Patch**
- Update `apps/openclaw-worker/lib/mission-dispatcher.js` (specifically `buildPrompt`).
- Instead of using a generic hardcoded mandate, the `buildPrompt` function must look up the matched Hand, extract its `systemPrompt`, and prepend it to the payload sent to the CC CLI pane.
- Format: `[ROLE: ${hand.name}] ${hand.systemPrompt} | Nhiệm vụ: ${taskContent}`.

**CONSTRAINTS:**
- 100% backward compatible with `brain-process-manager.js` and `factory-loop.sh`.
- DO NOT touch tmux pane allocation logic. Rely strictly on the 3 existing panes.
- Log the selected Role gracefully in the terminal.
"
