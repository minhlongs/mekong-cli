/plan:hard "
MISSION: Implement Phase 12 – The Omega Point – the final evolution where our algo-trader becomes a self-sustaining, self-evolving financial entity that is part of the market infrastructure. Create three advanced modules under 'apps/algo-trader/src/arbitrage/phase12_omega/' as detailed below. All code must be in TypeScript, with 100% Jest test coverage (using mocks for external services, blockchain, and hardware). Integrate with the existing core strategy engine via middleware hooks and configuration flags. Provide a configuration file 'config.phase12.json' at the project root.

**MODULE 1: Autopoietic Code Evolution**
- File path: `phase12_omega/autopoieticEngine/`
- Subcomponents:
  - `codeAnalyzer.ts`: Scans the entire codebase (`src/`), extracting metrics: complexity, test coverage, performance bottlenecks (via profiling logs). Outputs a structured report.
  - `llmCodeGenerator.ts`: Uses a fine‑tuned LLM (e.g., CodeLlama) to generate improved versions of modules. For simulation, a rule‑based refactorer makes simple optimizations (e.g., inlining functions, loop unrolling).
  - `sandboxExecutor.ts`: Compiles and runs the new code in an isolated Docker container against historical market data, comparing PnL, latency, and risk metrics against the current version.
  - `evolutionDecider.ts`: If the new version outperforms the current on all key metrics, it creates a pull request (or directly replaces files) and logs the change. Otherwise, discards.
  - `versionController.ts`: Maintains a git history of all auto‑generated versions, allowing rollback if needed.
  - `index.ts`: Runs the evolution loop on a schedule (e.g., monthly).
- Tests: Mock LLM responses, verify sandbox execution and decision logic. 100% coverage.

**MODULE 2: Self‑Sustaining Energy Arbitrage**
- File path: `phase12_omega/energyArbitrage/`
- Subcomponents:
  - `energyMarketConnector.ts`: Fetches real‑time electricity prices from spot markets (Nord Pool, ERCOT) via mock APIs. For simulation, use a CSV of historical prices.
  - `computeCostModel.ts`: Estimates current compute cost based on cloud provider rates (AWS, GCP) and actual usage (CPU hours, bandwidth).
  - `arbitrageOptimizer.ts`: Finds opportunities to buy energy when cheap and either resell (if market allows) or use for mining/validation. Outputs a schedule for compute‑intensive tasks.
  - `miningValidatorModule.ts`: Interfaces with mining pools (for PoW) or staking contracts (PoS) to redirect excess energy/compute. For simulation, just logs potential earnings.
  - `treasuryManager.ts`: Automatically allocates a portion of trading profits to cover operational costs, and reinvests surplus into more hardware or staking.
  - `index.ts`: Runs continuously, adjusting compute usage based on energy prices.
- Tests: Mock energy prices, verify cost calculations and optimization. 100% coverage.

**MODULE 3: Market Morphogenesis**
- File path: `phase12_omega/marketMorph/`
- Subcomponents:
  - `dexDeployer.ts`: Deploys a Uniswap V2‑style DEX on a testnet (or mainnet fork) with initial liquidity provided by the AGI’s treasury. For simulation, a mock contract.
  - `liquidityProvider.ts`: Manages the AGI’s LP positions on its own DEX and other AMMs, optimizing for fee yield and IL.
  - `validatorNode.ts`: Sets up a validator client for a PoS blockchain (Ethereum, Solana) using the AGI’s stake. For simulation, a mock client that reports rewards.
  - `orderFlowCapture.ts`: As a DEX and validator, the AGI earns fees from every trade and block. This module aggregates all revenue streams.
  - `infrastructureGovernor.ts`: Adjusts fees on the DEX, validator commission, and liquidity allocations based on market conditions and AGI’s own needs.
  - `index.ts`: Bootstraps the infrastructure components and starts the revenue loop.
- Tests: Mock contract interactions, verify revenue aggregation and fee adjustments. 100% coverage.

**Integration & Configuration**:
- Create `config.phase12.json` with the following schema:
  ```json
  {
    "autopoieticEngine": {
      "enabled": false,
      "evolutionIntervalDays": 30,
      "llmModel": "codellama-13b",
      "sandboxMemoryGb": 4
    },
    "energyArbitrage": {
      "enabled": false,
      "energyMarkets": ["nordpool", "ercot"],
      "cloudProviders": ["aws", "gcp"],
      "miningPool": "stratum+tcp://...",
      "stakingAddress": "0x..."
    },
    "marketMorph": {
      "enabled": false,
      "dexFactory": "0x...",
      "initialLiquidityUsd": 10000,
      "validatorKey": "./validator_keys",
      "commissionBps": 5
    }
  }
  ```
- Modify the main orchestrator (`arbitrageOrchestrator.ts`) to conditionally start each Phase 12 module based on the config.
- Expose metrics via WebSocket:
  - Autopoietic Engine: evolution runs, codebase metrics, PRs created.
  - Energy Arbitrage: current compute cost, energy price, profit margin.
  - Market Morph: DEX volume, LP yield, validator rewards.
- Add dashboard panels in NextJS to display these metrics (placeholders accepted).

**Testing & Quality**:
- Achieve 100% Jest coverage for all TypeScript files. Use `jest.mock` for LLMs, hardware/cloud interfaces, and external markets.
- For `autopoieticEngine`, provide an AST‑based mock refactorer to test sandbox compilation and decision logic.
- For `energyArbitrage`, simulate historical energy rate curves.
- For `marketMorph`, use a local blockchain fork (Hardhat) to verify contract deployments and revenue calculations.
- Ensure backward compatibility: if any module fails or is disabled, the system continues with previous phases.

**Implementation Notes**:
- `codeAnalyzer` could use `ts-morph` or `esprima` to gather metrics.
- The Git version controller should be careful not to push broken code; the PR flow is preferred for safety.
- For the local DEX, deploy generic AMM contracts and mock a swap feed.
- All modules must log extensively and support dry‑run mode (no real execution/deployments).

Proceed with implementation.
"
