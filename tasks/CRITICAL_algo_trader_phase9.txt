/plan:hard "
MISSION: Implement Phase 9 – The Singularity Engine – to achieve the absolute endgame of algorithmic trading. Create three advanced modules under 'apps/algo-trader/src/arbitrage/phase9_singularity/' as detailed below. All code must be in TypeScript, with 100% Jest test coverage (using mocks for external services). Integrate with the existing core strategy engine via middleware hooks and configuration flags. Provide a configuration file 'config.phase9.json' at the project root.

**MODULE 1: Quantum-Safe Vault & Post-Quantum Execution Layer (QSV)**
- File path: `phase9_singularity/quantumSafeVault/`
- Subcomponents:
  - `pqcKeyManager.ts`: Generates and stores post‑quantum key pairs (Dilithium, Kyber) using a WebAssembly module (e.g., `liboqs` compiled to WASM). Provides functions to sign messages and encapsulate shared secrets.
  - `hybridEncryptor.ts`: For network traffic, implements a hybrid key exchange (X25519 + Kyber) to establish a session key, then encrypts with AES‑256‑GCM. Wraps the existing exchange API clients to use this encryption when sending sensitive data.
  - `withdrawalSigner.ts`: Replaces standard ECDSA signatures for fund withdrawals with Dilithium signatures. Integrates with multi‑sig smart contracts that support post‑quantum verification (simulated via a mock contract for testing).
  - `hsmSimulator.ts`: Simulates a hardware security module interface for storing keys securely. For testing, uses in‑memory encrypted storage.
  - `forwardSecrecy.ts`: Implements a ratcheting mechanism (like Signal protocol) to ensure past messages cannot be decrypted if long‑term keys are compromised.
  - `index.ts`: Initializes the module and exposes a middleware to wrap all outbound communications.
- Tests: Mock `liboqs` WASM, verify signature generation/verification, hybrid encryption, and withdrawal flow. 100% coverage.

**MODULE 2: Neural-Symbolic Strategy Synthesizer (NS3)**
- File path: `phase9_singularity/neuralSymbolicSynthesizer/`
- Subcomponents:
  - `grammar.ts`: Defines a context‑free grammar for trading strategies (e.g., `Expr ::= price | indicator | Expr '+' Expr | ...`). This grammar guides the search space.
  - `populationManager.ts`: Maintains a population of strategy expressions (as ASTs) with fitness scores. Uses a genetic programming loop: selection, crossover, mutation.
  - `fitnessPredictor.ts`: A neural network (TensorFlow.js) that predicts the fitness of a new strategy without full backtesting, speeding up evolution. Trained on previous backtest results.
  - `backtestEngine.ts`: Executes a strategy on historical data (stored in Parquet/CSV) and computes Sharpe, drawdown, profit.
  - `codeGenerator.ts`: Converts a successful AST into a TypeScript module (e.g., `generatedStrategy_<hash>.ts`) with the proper interface, ready for hot‑loading.
  - `evolutionOrchestrator.ts`: Runs the evolution loop continuously in a background thread. Periodically promotes top strategies to live.
  - `index.ts`: Starts the synthesizer.
- Tests: Mock backtest results, verify genetic operators, code generation, and neural predictor. 100% coverage.

**MODULE 3: Omni-Macro Sentient Oracle (OMSO)**
- File path: `phase9_singularity/omniMacroOracle/`
- Subcomponents:
  - `newsIngestor.ts`: Connects to multiple news APIs (mock for testing) and streams headlines in real time. Uses a simple queue to manage rate limits.
  - `llmProcessor.ts`: Loads a fine‑tuned LLM (e.g., Llama‑3 8B) via `llama.cpp` or `transformers.js`. Processes batches of headlines to extract sentiment, entities, and event probabilities. Outputs a structured JSON object.
  - `macroSignalAggregator.ts`: Combines LLM outputs with on‑chain data (whale movements, exchange flows) and economic indicators (from APIs) to produce a unified macro state vector.
  - `signalBroadcaster.ts`: Publishes the macro state via a dedicated WebSocket channel that all trading modules subscribe to.
  - `modelUpdater.ts`: Periodically fine‑tunes the LLM on new data (if enabled). For simulation, uses a static model.
  - `index.ts`: Starts the ingestion and processing pipeline.
- Tests: Mock news feeds and LLM responses, verify aggregation and broadcasting. 100% coverage.

**Integration & Configuration**:
- Create `config.phase9.json` with the following schema:
  ```json
  {
    "quantumSafeVault": {
      "enabled": false,
      "pqcAlgorithm": "Dilithium5",
      "useHsm": false,
      "encryptTraffic": true
    },
    "neuralSymbolicSynthesizer": {
      "enabled": false,
      "populationSize": 1000,
      "evolutionIntervalMin": 60,
      "promotionThreshold": 1.5
    },
    "omniMacroOracle": {
      "enabled": false,
      "llmModel": "llama3-8b-instruct",
      "newsSources": ["reuters", "bloomberg", "twitter"],
      "updateIntervalSec": 10
    }
  }
  ```
- Modify the main orchestrator (`arbitrageOrchestrator.ts`) to conditionally start each Phase 9 module based on the config.
- Expose metrics via WebSocket:
  - QSV: keys managed, messages encrypted.
  - NS3: current population size, best fitness score, strategies generated.
  - OMSO: news ingested, sentiment score, macro signals emitted.
- Add dashboard panels in NextJS to display these metrics (placeholders accepted).

**Testing & Quality**:
- Achieve 100% Jest coverage for all TypeScript files.
- For QSV, substitute webassembly logic with mocks during test.
- Ensure backward compatibility: if any module fails or is disabled, the system continues with previous phases.

Proceed with implementation.
"
