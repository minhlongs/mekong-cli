/plan:hard "
MISSION TUYỆT ĐỐI: Biến Mekong CLI + CTO thành hệ thống ZERO-TO-SHIP.

CONTEXT: Chủ Tịch đã chờ 1 tuần mà CTO không ship được dự án nào. factory-loop.sh chỉ biết inject /bootstrap:auto:parallel — quét linh tinh, không bao giờ SHIP. Phải sửa NGAY.

CTO hiện tại có 3 CC CLI panes:
  P1 = apps/algo-trader
  P2 = apps/sophia-ai-factory  
  P3 = apps/well

PHẢI LÀM:

**1. SHIP PIPELINE trong factory-loop.sh**
Thay vì chỉ inject /bootstrap:auto:parallel, factory-loop phải có 5 PHASE tuần tự cho mỗi pane:
  Phase 1: /scout — scan project health, tìm lỗi critical
  Phase 2: /cook — fix tất cả lỗi critical tìm được
  Phase 3: /test — chạy toàn bộ test suite
  Phase 4: /review:codebase — audit code quality + security
  Phase 5: /check-and-commit — commit + push nếu tất cả GREEN

Sau Phase 5 xong, tạo handover report tự động (file HANDOVER_REPORT.md trong project).

factory-loop.sh phải track PHASE hiện tại của từng pane (lưu vào .tom_hum_state.json).
Khi pane idle, inject PHASE tiếp theo, không inject bootstrap nữa.

**2. PROJECT HEALTH SCORING**
Trước khi dispatch, CTO phải chấm điểm project:
  - Build OK? (npm run build)
  - Tests pass? (npm test)  
  - No critical lint errors?
  - README.md exists & updated?
  - Production URL reachable? (curl)
Score < 70 → fix mode. Score >= 90 → ship mode.

**3. HANDOVER REPORT GENERATOR**
Khi project đạt score >= 90, tự động tạo:
  - HANDOVER_REPORT.md: tóm tắt features, test results, deployment info
  - PRODUCTION_CHECKLIST.md: checklist 20 items bàn giao khách

**4. Sửa factory-loop.sh**
File: /Users/macbookprom1/mekong-cli/factory-loop.sh
Thay logic cũ (chỉ inject bootstrap) bằng SHIP PIPELINE 5 phase.

CONSTRAINT: Giữ nguyên 3 pane (P1, P2, P3). KHÔNG spawn thêm pane. Backward compatible.
OUTPUT: Code chạy được, không phải plan. Commit + push khi xong.
"
