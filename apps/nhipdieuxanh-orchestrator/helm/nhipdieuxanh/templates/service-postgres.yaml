apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  labels:
    app: postgres
spec:
  ports:
    - port: {{ .Values.postgres.port }}
      targetPort: postgres
      protocol: TCP
  selector:
    app: postgres
