apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  labels:
    app: kafka
spec:
  ports:
    - port: {{ .Values.kafkaBroker.port }}
      targetPort: kafka
      protocol: TCP
  selector:
    app: kafka
