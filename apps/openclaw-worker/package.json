{
	"name": "openclaw-worker",
	"version": "2026.2.13",
	"private": true,
	"description": "Autonomous AI Swarm Daemon for Mekong CLI",
	"author": "Binh Phap Venture Studio",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/longtho638-jpg/mekong-cli.git",
		"directory": "apps/openclaw-worker"
	},
	"scripts": {
		"build": "node --check task-watcher.js && node --check config.js && node --check bridge-server.js && echo '✅ Syntax OK'",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"test:record": "vitest && node scribe-daemon.js --record-win",
		"set-brain": "bash scripts/set-brain.sh",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.62.0"
	}
}