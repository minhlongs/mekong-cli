{
	"name": "openclaw-worker",
	"version": "2026.2.9",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"test:record": "vitest && node scribe-daemon.js --record-win",
		"set-brain": "bash scripts/set-brain.sh",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.62.0"
	}
}