COMPLEXITY: medium
PROJECT: well

/cook "FIX NETWORK TREE RECURSION SAFETY + FLOATING POINT.

1. TreeNode — thêm maxDepth safety (max 10 levels):
   src/components/network-tree/TreeNode.tsx — hiện recursive không giới hạn depth
   Thêm: if (level >= MAX_DEPTH) return null;

2. Floating point — wallet staking reward:
   src/components/Wallet/wallet-token-balance-card.tsx:170 — .toFixed(2) trên floating point
   Dùng Math.round(value * 100) / 100 thay vì .toFixed()

3. Vi.ts missing keys:
   src/locales/vi.ts — 28 keys có giá trị '[MISSING]'
   Dịch và thay thế tất cả

npm run build && npm test verify. Respond tiếng Việt." --auto
