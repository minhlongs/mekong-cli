COMPLEXITY: high
PROJECT: well

/cook "FIX ADMIN AUTH GUARD — Admin pages thiếu auth check.

src/pages/Admin/ — KHÔNG CÓ useAuth hoặc isAdmin check.
MỌI admin page PHẢI check isAdmin trước khi render.
Nếu user không phải admin → redirect về /dashboard.

Kiểm tra:
1. grep -rn 'useAuth\|isAdmin' src/pages/Admin/ --include='*.tsx'
2. Thêm auth guard cho MỌI admin page nếu thiếu
3. Kiểm tra src/components/admin/ — tất cả admin components phải có auth check

Fix tất cả. npm run build verify. Respond tiếng Việt." --auto
