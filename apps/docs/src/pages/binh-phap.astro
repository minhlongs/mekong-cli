---
import LandingLayout from '../layouts/LandingLayout.astro';

const title = '13 Binh Pháp - AgencyOS';
const description = 'Sun Tzu\'s Art of War applied to business. 13 strategic chapters mapped to AI commands.';
---

<LandingLayout title={title} description={description}>
  <section class="binh-phap-hero">
    <span class="hero-icon">🏯</span>
    <h1>13 Binh Pháp</h1>
    <p class="tagline">"Bất chiến nhi khuất nhân chi binh" - Win Without Fighting</p>
    <p class="subtitle">Sun Tzu's Art of War × AI-Powered Commands</p>
  </section>

  <section class="chapters-grid">
    
    <!-- Chapter 1 -->
    <div class="chapter-card">
      <div class="chapter-number">1</div>
      <div class="chapter-content">
        <h3>始計 Kế Hoạch</h3>
        <p class="chapter-purpose">Lập chiến lược</p>
        <div class="commands">
          <code>/plan</code>
          <code>/binh-phap</code>
          <code>/okr</code>
        </div>
      </div>
    </div>

    <!-- Chapter 2 -->
    <div class="chapter-card">
      <div class="chapter-number">2</div>
      <div class="chapter-content">
        <h3>作戰 Tác Chiến</h3>
        <p class="chapter-purpose">Vận hành</p>
        <div class="commands">
          <code>/ops</code>
          <code>/billing</code>
          <code>/reporting</code>
        </div>
      </div>
    </div>

    <!-- Chapter 3 -->
    <div class="chapter-card">
      <div class="chapter-number">3</div>
      <div class="chapter-content">
        <h3>謀攻 Mưu Công</h3>
        <p class="chapter-purpose">Tăng trưởng nhẹ</p>
        <div class="commands">
          <code>/affiliate</code>
          <code>/growth</code>
        </div>
      </div>
    </div>

    <!-- Chapter 4 -->
    <div class="chapter-card">
      <div class="chapter-number">4</div>
      <div class="chapter-content">
        <h3>形勢 Hình Thế</h3>
        <p class="chapter-purpose">Bảo vệ lợi thế</p>
        <div class="commands">
          <code>/moat</code>
          <code>/audit</code>
        </div>
      </div>
    </div>

    <!-- Chapter 5 -->
    <div class="chapter-card">
      <div class="chapter-number">5</div>
      <div class="chapter-content">
        <h3>勢 Thế Trận</h3>
        <p class="chapter-purpose">Bắt trend</p>
        <div class="commands">
          <code>/trend</code>
          <code>/scout-keo</code>
        </div>
      </div>
    </div>

    <!-- Chapter 6 -->
    <div class="chapter-card">
      <div class="chapter-number">6</div>
      <div class="chapter-content">
        <h3>虛實 Hư Thực</h3>
        <p class="chapter-purpose">Chống pha loãng</p>
        <div class="commands">
          <code>/equity</code>
          <code>/cap-table</code>
        </div>
      </div>
    </div>

    <!-- Chapter 7 -->
    <div class="chapter-card highlight">
      <div class="chapter-number">7</div>
      <div class="chapter-content">
        <h3>軍爭 Quân Tranh</h3>
        <p class="chapter-purpose">Ship nhanh</p>
        <div class="commands">
          <code>/sprint</code>
          <code>/ship</code>
          <code>/code</code>
        </div>
      </div>
    </div>

    <!-- Chapter 8 -->
    <div class="chapter-card">
      <div class="chapter-number">8</div>
      <div class="chapter-content">
        <h3>九變 Cửu Biến</h3>
        <p class="chapter-purpose">Pivot</p>
        <div class="commands">
          <code>/pivot</code>
          <code>/experiment</code>
        </div>
      </div>
    </div>

    <!-- Chapter 9 -->
    <div class="chapter-card">
      <div class="chapter-number">9</div>
      <div class="chapter-content">
        <h3>行軍 Hành Quân</h3>
        <p class="chapter-purpose">Thực thi</p>
        <div class="commands">
          <code>/kpi</code>
          <code>/standup</code>
        </div>
      </div>
    </div>

    <!-- Chapter 10 -->
    <div class="chapter-card">
      <div class="chapter-number">10</div>
      <div class="chapter-content">
        <h3>地形 Địa Hình</h3>
        <p class="chapter-purpose">Mở rộng thị trường</p>
        <div class="commands">
          <code>/market</code>
          <code>/launch</code>
          <code>/sales</code>
        </div>
      </div>
    </div>

    <!-- Chapter 11 -->
    <div class="chapter-card danger">
      <div class="chapter-number">11</div>
      <div class="chapter-content">
        <h3>九地 Cửu Địa</h3>
        <p class="chapter-purpose">Xử lý khủng hoảng</p>
        <div class="commands">
          <code>/crisis</code>
          <code>/survival</code>
        </div>
      </div>
    </div>

    <!-- Chapter 12 -->
    <div class="chapter-card fire">
      <div class="chapter-number">12</div>
      <div class="chapter-content">
        <h3>火攻 Hỏa Công</h3>
        <p class="chapter-purpose">Viral marketing</p>
        <div class="commands">
          <code>/viral</code>
          <code>/video</code>
          <code>/marketing</code>
        </div>
      </div>
    </div>

    <!-- Chapter 13 -->
    <div class="chapter-card intel">
      <div class="chapter-number">13</div>
      <div class="chapter-content">
        <h3>用間 Dụng Gián</h3>
        <p class="chapter-purpose">Thu thập intel</p>
        <div class="commands">
          <code>/intel</code>
          <code>/fundraise</code>
        </div>
      </div>
    </div>

  </section>

  <section class="wisdom-section">
    <h2>🎯 Core Wisdom</h2>
    <div class="wisdom-grid">
      <div class="wisdom-card">
        <blockquote>"Bách chiến bách thắng, phi thiện chi thiện giả dã"</blockquote>
        <p>Trăm trận trăm thắng không phải hay nhất</p>
      </div>
      <div class="wisdom-card">
        <blockquote>"Bất chiến nhi khuất nhân chi binh, thiện chi thiện giả dã"</blockquote>
        <p>Không đánh mà thắng mới là hay nhất</p>
      </div>
    </div>
  </section>

  <section class="cta-section">
    <h2>Apply ancient wisdom to modern business</h2>
    <a href="/commands" class="btn-primary">View All Commands →</a>
  </section>
</LandingLayout>

<style>
  /* ===================== HERO WOW ===================== */
  .binh-phap-hero {
    text-align: center;
    padding: 64px 24px 32px;
    animation: hero-entrance 0.8s ease-out;
  }

  @keyframes hero-entrance {
    from { opacity: 0; transform: translateY(-30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .hero-icon {
    font-size: 4rem;
    display: block;
    margin-bottom: 16px;
    animation: icon-float 3s ease-in-out infinite, fire-glow 2s ease-in-out infinite;
    filter: drop-shadow(0 0 20px rgba(234, 179, 8, 0.5));
  }

  @keyframes icon-float {
    0%, 100% { transform: translateY(0) rotate(-3deg); }
    50% { transform: translateY(-10px) rotate(3deg); }
  }

  @keyframes fire-glow {
    0%, 100% { filter: drop-shadow(0 0 20px rgba(234, 179, 8, 0.5)) drop-shadow(0 0 40px rgba(249, 115, 22, 0.3)); }
    50% { filter: drop-shadow(0 0 35px rgba(234, 179, 8, 0.8)) drop-shadow(0 0 60px rgba(249, 115, 22, 0.5)); }
  }

  .binh-phap-hero h1 {
    font-size: 3rem;
    font-weight: 800;
    background: linear-gradient(135deg, #eab308, #f97316, #ef4444, #eab308);
    background-size: 300% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 16px;
    animation: title-shine 4s linear infinite;
  }

  @keyframes title-shine {
    0% { background-position: 0% center; }
    100% { background-position: 300% center; }
  }

  .tagline {
    font-size: 1.25rem;
    color: var(--color-text-muted);
    font-style: italic;
    margin-bottom: 8px;
    animation: fade-up 0.6s ease-out 0.2s both;
  }

  .subtitle {
    font-size: 1rem;
    color: var(--color-text-muted);
    animation: fade-up 0.6s ease-out 0.3s both;
  }

  @keyframes fade-up {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ===================== CHAPTERS GRID WOW ===================== */
  .chapters-grid {
    max-width: 1200px;
    margin: 0 auto;
    padding: 24px;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 16px;
  }

  .chapter-card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 20px;
    display: flex;
    gap: 16px;
    transition: all 0.3s ease;
    animation: card-entrance 0.5s ease-out both;
    position: relative;
    overflow: hidden;
  }

  /* Staggered entrance for each card */
  .chapter-card:nth-child(1) { animation-delay: 0.1s; }
  .chapter-card:nth-child(2) { animation-delay: 0.15s; }
  .chapter-card:nth-child(3) { animation-delay: 0.2s; }
  .chapter-card:nth-child(4) { animation-delay: 0.25s; }
  .chapter-card:nth-child(5) { animation-delay: 0.3s; }
  .chapter-card:nth-child(6) { animation-delay: 0.35s; }
  .chapter-card:nth-child(7) { animation-delay: 0.4s; }
  .chapter-card:nth-child(8) { animation-delay: 0.45s; }
  .chapter-card:nth-child(9) { animation-delay: 0.5s; }
  .chapter-card:nth-child(10) { animation-delay: 0.55s; }
  .chapter-card:nth-child(11) { animation-delay: 0.6s; }
  .chapter-card:nth-child(12) { animation-delay: 0.65s; }
  .chapter-card:nth-child(13) { animation-delay: 0.7s; }

  @keyframes card-entrance {
    from { opacity: 0; transform: translateY(20px) scale(0.95); }
    to { opacity: 1; transform: translateY(0) scale(1); }
  }

  .chapter-card:hover {
    transform: translateY(-4px);
    border-color: #eab308;
    box-shadow: 0 8px 25px rgba(234, 179, 8, 0.15);
  }

  /* Shine sweep on hover */
  .chapter-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(234, 179, 8, 0.08), transparent);
    transition: left 0.5s ease;
    pointer-events: none;
  }

  .chapter-card:hover::before {
    left: 100%;
  }

  /* Special chapter styles */
  .chapter-card.highlight {
    border-color: #22c55e;
    background: linear-gradient(135deg, rgba(34, 197, 94, 0.08), transparent);
  }
  .chapter-card.highlight:hover {
    box-shadow: 0 8px 25px rgba(34, 197, 94, 0.2);
  }

  .chapter-card.danger {
    border-color: #ef4444;
    background: linear-gradient(135deg, rgba(239, 68, 68, 0.08), transparent);
    animation: card-entrance 0.5s ease-out 0.6s both, danger-pulse 3s ease-in-out infinite 1s;
  }
  @keyframes danger-pulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(239, 68, 68, 0); }
    50% { box-shadow: 0 0 20px 3px rgba(239, 68, 68, 0.15); }
  }

  .chapter-card.fire {
    border-color: #f97316;
    background: linear-gradient(135deg, rgba(249, 115, 22, 0.08), transparent);
    animation: card-entrance 0.5s ease-out 0.65s both, fire-flicker 0.5s ease-in-out infinite;
  }
  @keyframes fire-flicker {
    0%, 100% { box-shadow: 0 0 15px 2px rgba(249, 115, 22, 0.2); }
    25% { box-shadow: 0 0 20px 4px rgba(249, 115, 22, 0.3); }
    75% { box-shadow: 0 0 12px 1px rgba(249, 115, 22, 0.15); }
  }

  .chapter-card.intel {
    border-color: #8b5cf6;
    background: linear-gradient(135deg, rgba(139, 92, 246, 0.08), transparent);
  }
  .chapter-card.intel:hover {
    box-shadow: 0 8px 25px rgba(139, 92, 246, 0.2);
  }

  /* ===================== CHAPTER NUMBER WOW ===================== */
  .chapter-number {
    width: 40px;
    height: 40px;
    background: var(--color-bg-tertiary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    color: #eab308;
    flex-shrink: 0;
    transition: all 0.3s ease;
    position: relative;
    z-index: 1;
  }

  .chapter-card:hover .chapter-number {
    transform: scale(1.15);
    box-shadow: 0 0 15px rgba(234, 179, 8, 0.4);
  }

  .chapter-card.highlight .chapter-number { color: #22c55e; }
  .chapter-card.highlight:hover .chapter-number { box-shadow: 0 0 15px rgba(34, 197, 94, 0.4); }
  
  .chapter-card.danger .chapter-number { color: #ef4444; }
  .chapter-card.danger:hover .chapter-number { box-shadow: 0 0 15px rgba(239, 68, 68, 0.4); }
  
  .chapter-card.fire .chapter-number { color: #f97316; }
  .chapter-card.fire:hover .chapter-number { box-shadow: 0 0 15px rgba(249, 115, 22, 0.4); }
  
  .chapter-card.intel .chapter-number { color: #8b5cf6; }
  .chapter-card.intel:hover .chapter-number { box-shadow: 0 0 15px rgba(139, 92, 246, 0.4); }

  /* ===================== CHAPTER CONTENT ===================== */
  .chapter-content h3 {
    font-size: 1rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0 0 4px 0;
  }

  .chapter-purpose {
    font-size: 0.8rem;
    color: var(--color-text-muted);
    margin: 0 0 12px 0;
  }

  .commands {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .commands code {
    background: var(--color-bg-tertiary);
    color: #eab308;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: 0.75rem;
    font-family: 'JetBrains Mono', monospace;
    transition: all 0.2s ease;
    cursor: pointer;
  }

  .commands code:hover {
    background: rgba(234, 179, 8, 0.15);
    transform: translateY(-2px);
    box-shadow: 0 3px 10px rgba(234, 179, 8, 0.2);
  }

  .chapter-card.highlight .commands code { color: #22c55e; }
  .chapter-card.highlight .commands code:hover { background: rgba(34, 197, 94, 0.15); box-shadow: 0 3px 10px rgba(34, 197, 94, 0.2); }
  
  .chapter-card.danger .commands code { color: #ef4444; }
  .chapter-card.danger .commands code:hover { background: rgba(239, 68, 68, 0.15); box-shadow: 0 3px 10px rgba(239, 68, 68, 0.2); }
  
  .chapter-card.fire .commands code { color: #f97316; }
  .chapter-card.fire .commands code:hover { background: rgba(249, 115, 22, 0.15); box-shadow: 0 3px 10px rgba(249, 115, 22, 0.2); }
  
  .chapter-card.intel .commands code { color: #8b5cf6; }
  .chapter-card.intel .commands code:hover { background: rgba(139, 92, 246, 0.15); box-shadow: 0 3px 10px rgba(139, 92, 246, 0.2); }

  /* ===================== WISDOM SECTION WOW ===================== */
  .wisdom-section {
    max-width: 800px;
    margin: 48px auto;
    padding: 24px;
    text-align: center;
    animation: fade-up 0.6s ease-out 1s both;
  }

  .wisdom-section h2 {
    font-size: 1.5rem;
    color: var(--color-text-primary);
    margin-bottom: 24px;
  }

  .wisdom-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 16px;
  }

  .wisdom-card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 24px;
    transition: all 0.3s ease;
  }

  .wisdom-card:hover {
    border-color: #eab308;
    box-shadow: 0 4px 20px rgba(234, 179, 8, 0.1);
    transform: translateY(-2px);
  }

  .wisdom-card blockquote {
    font-size: 1rem;
    font-style: italic;
    background: linear-gradient(135deg, #eab308, #f97316);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin: 0 0 12px 0;
  }

  .wisdom-card p {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    margin: 0;
  }

  /* ===================== CTA SECTION WOW ===================== */
  .cta-section {
    text-align: center;
    padding: 64px 24px;
    background: linear-gradient(180deg, transparent, rgba(234, 179, 8, 0.05));
    border-top: 1px solid var(--color-border);
    animation: fade-up 0.6s ease-out 1.2s both;
  }

  .cta-section h2 {
    font-size: 2rem;
    color: var(--color-text-primary);
    margin-bottom: 24px;
  }

  .btn-primary {
    display: inline-block;
    background: linear-gradient(135deg, #eab308, #f97316);
    color: white;
    padding: 14px 32px;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    animation: flame-pulse 2s ease-in-out infinite;
    position: relative;
    overflow: hidden;
  }

  @keyframes flame-pulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(234, 179, 8, 0.4), 0 0 20px rgba(249, 115, 22, 0.2); }
    50% { box-shadow: 0 0 25px 5px rgba(234, 179, 8, 0.3), 0 0 40px rgba(249, 115, 22, 0.3); }
  }

  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(234, 179, 8, 0.4);
  }

  /* ===================== RESPONSIVE ===================== */
  @media (max-width: 768px) {
    .binh-phap-hero h1 { font-size: 2rem; }
    .chapters-grid { grid-template-columns: 1fr; }
  }
</style>
