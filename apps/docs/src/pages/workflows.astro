---
import LandingLayout from '../layouts/LandingLayout.astro';

const title = 'Money Workflows - AgencyOS';
const description = '3 proven workflows to make money with AgencyOS: Affiliate, Code-to-Content, and Consulting.';
---

<LandingLayout title={title} description={description}>
  <section class="workflows-hero">
    <h1>💰 Money-Making Workflows</h1>
    <p>Step-by-step paths to revenue</p>
  </section>

  <!-- Workflow 1: Affiliate -->
  <section class="workflow-section">
    <div class="workflow-card affiliate">
      <div class="workflow-header">
        <span class="workflow-number">01</span>
        <div>
          <h2>Affiliate-as-SaaS</h2>
          <span class="income-badge">Passive Income: $500-2,000/month</span>
        </div>
      </div>

      <div class="workflow-flow">
        <div class="flow-step">
          <code>/affiliate/scout</code>
          <span>Tìm programs</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <code>Chọn 5 programs</code>
          <span>PayPal-friendly</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <code>/marketing/social</code>
          <span>Viết 30 posts</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <code>Post daily x30</code>
          <span>Schedule & post</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step result">
          <span class="money">💰 $500-2,000</span>
          <span>Monthly passive</span>
        </div>
      </div>

      <div class="workflow-steps">
        <h3>Steps:</h3>
        <ol>
          <li><code>/affiliate/scout</code> → Tìm programs PayPal-friendly</li>
          <li><code>/marketing/social facebook</code> → Viết 30 posts</li>
          <li>Schedule posts → Đăng 1 post/ngày</li>
          <li>Track → Hoa hồng về account</li>
        </ol>
      </div>
    </div>
  </section>

  <!-- Workflow 2: Code-to-Content -->
  <section class="workflow-section">
    <div class="workflow-card code-content">
      <div class="workflow-header">
        <span class="workflow-number">02</span>
        <div>
          <h2>Code-to-Content</h2>
          <span class="income-badge">Active Income: AdSense + Sponsors</span>
        </div>
      </div>

      <div class="workflow-flow">
        <div class="flow-step">
          <code>/code</code>
          <span>Build feature</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <code>/video/script</code>
          <span>Create script</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <code>/video</code>
          <span>Render video</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <code>YouTube + TikTok</code>
          <span>Upload multi-platform</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step result">
          <span class="money">💰 $$$</span>
          <span>AdSense + Sponsors</span>
        </div>
      </div>

      <div class="workflow-steps">
        <h3>Steps:</h3>
        <ol>
          <li><code>/code</code> → Build feature mới</li>
          <li><code>/video/script</code> → Convert code thành script</li>
          <li><code>/video</code> → Render với voiceover</li>
          <li>Upload đa platform → $$$</li>
        </ol>
      </div>
    </div>
  </section>

  <!-- Workflow 3: Consulting -->
  <section class="workflow-section">
    <div class="workflow-card consulting">
      <div class="workflow-header">
        <span class="workflow-number">03</span>
        <div>
          <h2>Consulting Deal Flow</h2>
          <span class="income-badge">High-Ticket: $5,000-50,000/deal</span>
        </div>
      </div>

      <div class="workflow-flow">
        <div class="flow-step">
          <code>/sales/lead</code>
          <span>Find leads</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <code>/intel/competitor</code>
          <span>Analyze market</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <code>/sales/proposal</code>
          <span>Create proposal</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <code>/sales/close</code>
          <span>Close deal</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step">
          <code>/finance/invoice</code>
          <span>Get paid</span>
        </div>
        <div class="flow-arrow">→</div>
        <div class="flow-step result">
          <span class="money">💰 $5-50K</span>
          <span>Per deal</span>
        </div>
      </div>

      <div class="workflow-steps">
        <h3>Steps:</h3>
        <ol>
          <li><code>/sales/lead</code> → Tìm khách tiềm năng</li>
          <li><code>/intel/competitor</code> → Phân tích đối thủ</li>
          <li><code>/sales/proposal</code> → Viết proposal tự động</li>
          <li><code>/sales/close</code> → Script closing deal</li>
          <li><code>/finance/invoice</code> → Xuất hóa đơn</li>
        </ol>
      </div>
    </div>
  </section>

  <section class="cta-section">
    <h2>Ready to start earning?</h2>
    <div class="cta-buttons">
      <a href="/quickstart" class="btn-primary">Pick Your Path →</a>
      <a href="/commands" class="btn-secondary">View All Commands</a>
    </div>
  </section>
</LandingLayout>

<style>
  /* ===================== HERO WOW ===================== */
  .workflows-hero {
    text-align: center;
    padding: 64px 24px 32px;
    animation: hero-entrance 0.8s ease-out;
  }

  @keyframes hero-entrance {
    from { opacity: 0; transform: translateY(-30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .workflows-hero h1 {
    font-size: 3rem;
    font-weight: 800;
    background: linear-gradient(135deg, #22c55e, #eab308, #22c55e);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 16px;
    animation: title-shine 4s linear infinite;
  }

  @keyframes title-shine {
    0% { background-position: 0% center; }
    100% { background-position: 200% center; }
  }

  .workflows-hero p {
    font-size: 1.25rem;
    color: var(--color-text-muted);
  }

  .workflow-section {
    max-width: 900px;
    margin: 0 auto;
    padding: 24px;
  }

  /* ===================== WORKFLOW CARD WOW ===================== */
  .workflow-card {
    background: var(--color-bg-secondary);
    border: 2px solid var(--color-border);
    border-radius: 16px;
    padding: 32px;
    transition: all 0.3s ease;
    animation: card-entrance 0.6s ease-out both;
  }

  .workflow-section:nth-child(2) .workflow-card { animation-delay: 0.3s; }
  .workflow-section:nth-child(3) .workflow-card { animation-delay: 0.4s; }
  .workflow-section:nth-child(4) .workflow-card { animation-delay: 0.5s; }

  @keyframes card-entrance {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .workflow-card:hover {
    transform: translateY(-6px) scale(1.01);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.2);
  }

  .workflow-card.affiliate:hover { border-color: #22c55e; }
  .workflow-card.code-content:hover { border-color: #3b82f6; }
  .workflow-card.consulting:hover { border-color: #eab308; }

  .workflow-header {
    display: flex;
    align-items: flex-start;
    gap: 16px;
    margin-bottom: 24px;
  }

  .workflow-number {
    background: var(--color-bg-tertiary);
    color: var(--color-text-muted);
    padding: 8px 16px;
    border-radius: 8px;
    font-weight: 700;
    font-size: 1.25rem;
  }

  .workflow-header h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0 0 8px 0;
  }

  .income-badge {
    background: linear-gradient(135deg, #22c55e, #16a34a);
    color: white;
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
    animation: badge-pulse 2s ease-in-out infinite;
  }

  @keyframes badge-pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }

  .workflow-flow {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
    margin-bottom: 24px;
    padding: 16px;
    background: var(--color-bg-tertiary);
    border-radius: 12px;
  }

  .flow-step {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: 4px;
  }

  .flow-step code {
    background: var(--color-bg-secondary);
    color: #22c55e;
    padding: 6px 12px;
    border-radius: 6px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
    border: 1px solid var(--color-border);
  }

  .flow-step span {
    font-size: 0.7rem;
    color: var(--color-text-muted);
  }

  .flow-step.result {
    background: var(--color-bg-secondary);
    padding: 12px 16px;
    border-radius: 8px;
    border: 2px solid #22c55e;
  }

  .flow-step.result .money {
    font-size: 1rem;
    font-weight: 700;
    color: #22c55e;
  }

  .flow-arrow {
    color: var(--color-text-muted);
    font-size: 1.25rem;
  }

  .workflow-steps {
    border-top: 1px solid var(--color-border);
    padding-top: 16px;
  }

  .workflow-steps h3 {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--color-text-muted);
    margin: 0 0 12px 0;
    text-transform: uppercase;
  }

  .workflow-steps ol {
    margin: 0;
    padding-left: 20px;
  }

  .workflow-steps li {
    color: var(--color-text-secondary);
    margin-bottom: 8px;
    font-size: 0.9rem;
  }

  .workflow-steps code {
    color: #22c55e;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.8rem;
  }

  /* ===================== CTA WOW ===================== */
  .cta-section {
    text-align: center;
    padding: 64px 24px;
    background: linear-gradient(180deg, transparent, rgba(34, 197, 94, 0.05));
    border-top: 1px solid var(--color-border);
    animation: cta-entrance 0.6s ease-out 0.7s both;
  }

  @keyframes cta-entrance {
    from { opacity: 0; transform: scale(0.95); }
    to { opacity: 1; transform: scale(1); }
  }

  .cta-section h2 {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin-bottom: 24px;
  }

  .cta-buttons {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-primary {
    background: linear-gradient(135deg, #22c55e, #16a34a);
    color: white;
    padding: 14px 32px;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    animation: btn-pulse 2.5s ease-in-out infinite;
  }

  @keyframes btn-pulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.4); }
    50% { box-shadow: 0 0 20px 5px rgba(34, 197, 94, 0.2); }
  }

  .btn-primary:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 12px 30px rgba(34, 197, 94, 0.4);
  }

  .btn-secondary {
    background: var(--color-bg-tertiary);
    color: var(--color-text-primary);
    padding: 14px 32px;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    border: 1px solid var(--color-border);
    transition: all 0.2s ease;
  }

  .btn-secondary:hover {
    background: var(--color-bg-secondary);
  }

  @media (max-width: 768px) {
    .workflows-hero h1 {
      font-size: 2rem;
    }

    .workflow-card {
      padding: 20px;
    }

    .workflow-flow {
      justify-content: center;
    }

    .flow-arrow {
      display: none;
    }
  }
</style>
