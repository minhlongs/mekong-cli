---
import LandingLayout from '../layouts/LandingLayout.astro';
import { getLocaleFromUrl, useTranslations, getRelativeLocaleUrl } from '@agencyos/i18n/astro';
import { locales } from '@agencyos/i18n/locales';

const locale = getLocaleFromUrl(Astro.url);
const t = useTranslations(locale);

const title = t('privacy_page.title');
const description = t('privacy_page.description');

const pageData = (locales[locale] as any).privacy_page;
---

<LandingLayout title={title} description={description}>
  <section class="legal-page">
    <div class="legal-container">

      <div class="legal-header">
        <h1>{pageData.hero.title}</h1>
        <p class="last-updated">{pageData.hero.last_updated}</p>
      </div>

      <div class="legal-content">

        <h2>{pageData.sections.collection.title}</h2>
        <p>{pageData.sections.collection.intro}</p>
        <ul>
          {pageData.sections.collection.items.map((item: string) => (
            <li>{item}</li>
          ))}
        </ul>

        <h2>{pageData.sections.usage.title}</h2>
        <ul>
          {pageData.sections.usage.items.map((item: string) => (
            <li>{item}</li>
          ))}
        </ul>

        <h2>{pageData.sections.security.title}</h2>
        <p>{pageData.sections.security.intro}</p>
        <ul>
          {pageData.sections.security.items.map((item: string) => (
            <li>{item}</li>
          ))}
        </ul>

        <h2>{pageData.sections.third_party.title}</h2>
        <p>{pageData.sections.third_party.intro}</p>
        <ul>
          {pageData.sections.third_party.items.map((item: string) => (
            <li>{item}</li>
          ))}
        </ul>

        <h2>{pageData.sections.rights.title}</h2>
        <ul>
          {pageData.sections.rights.items.map((item: string) => (
            <li>{item}</li>
          ))}
        </ul>

        <h2>{pageData.sections.cookies.title}</h2>
        <p>{pageData.sections.cookies.intro}</p>
        <ul>
          {pageData.sections.cookies.items.map((item: string) => (
            <li>{item}</li>
          ))}
        </ul>
        <p>{pageData.sections.cookies.outro}</p>

        <h2>{pageData.sections.contact.title}</h2>
        <p>{pageData.sections.contact.intro}</p>
        <ul>
          <li>{pageData.sections.contact.email}</li>
        </ul>

      </div>

      <div class="legal-footer">
        <a href={getRelativeLocaleUrl(locale, '/terms')}>{pageData.footer.terms}</a>
      </div>

    </div>
  </section>
</LandingLayout>

<style>
  .legal-page {
    min-height: calc(100vh - 72px);
    padding: 64px 24px;
  }

  .legal-container {
    max-width: 700px;
    margin: 0 auto;
  }

  /* ===================== HEADER WOW ===================== */
  .legal-header {
    text-align: center;
    margin-bottom: 48px;
    animation: hero-entrance 0.8s ease-out;
  }

  @keyframes hero-entrance {
    from { opacity: 0; transform: translateY(-20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .legal-header h1 {
    font-size: 2.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #22c55e, #06b6d4, #8b5cf6, #22c55e);
    background-size: 300% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 8px;
    animation: title-shine 5s linear infinite;
  }

  @keyframes title-shine {
    0% { background-position: 0% center; }
    100% { background-position: 300% center; }
  }

  .last-updated {
    font-size: 0.875rem;
    color: var(--color-text-muted);
  }

  /* ===================== CONTENT WOW ===================== */
  .legal-content {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 32px;
    animation: content-fade 0.6s ease-out 0.2s both;
  }

  @keyframes content-fade {
    from { opacity: 0; transform: translateY(15px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .legal-content h2 {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin-top: 24px;
    margin-bottom: 12px;
  }

  .legal-content h2:first-child {
    margin-top: 0;
  }

  .legal-content p {
    color: var(--color-text-secondary);
    margin-bottom: 12px;
    line-height: 1.6;
  }

  .legal-content ul {
    margin-bottom: 16px;
    padding-left: 24px;
  }

  .legal-content li {
    color: var(--color-text-secondary);
    margin-bottom: 8px;
    line-height: 1.5;
  }

  .legal-content a {
    color: #22c55e;
    transition: all 0.2s ease;
  }

  .legal-content a:hover {
    text-decoration: underline;
  }

  /* ===================== FOOTER WOW ===================== */
  .legal-footer {
    text-align: center;
    margin-top: 32px;
    animation: content-fade 0.6s ease-out 0.4s both;
  }

  .legal-footer a {
    color: #22c55e;
    font-weight: 500;
    transition: all 0.2s ease;
  }

  .legal-footer a:hover {
    transform: translateX(4px);
    display: inline-block;
  }
</style>
