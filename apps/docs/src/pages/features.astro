---
import LandingLayout from '../layouts/LandingLayout.astro';

const title = 'Features - AgencyOS';
const description = '85+ AI-powered commands to automate your entire agency. Marketing, sales, operations, finance.';
---

<LandingLayout title={title} description={description}>
  <section class="features-hero">
    <h1>85+ Commands.<br/>One Platform.</h1>
    <p>Everything you need to run a solo agency with AI.</p>
  </section>

  <section class="features-categories">
    
    <!-- Marketing -->
    <div class="category">
      <div class="category-header">
        <span class="category-icon">📢</span>
        <h2>Marketing</h2>
        <span class="command-count">15 commands</span>
      </div>
      <div class="commands-grid">
        <div class="command-card">
          <code>/ke-hoach-tiep-thi</code>
          <p>Create marketing plans</p>
        </div>
        <div class="command-card">
          <code>/noi-dung-tiep-thi</code>
          <p>Generate content</p>
        </div>
        <div class="command-card">
          <code>/y-tuong-social-media</code>
          <p>Social media ideas</p>
        </div>
        <div class="command-card">
          <code>/ke-hoach-pr</code>
          <p>PR campaigns</p>
        </div>
        <div class="command-card">
          <code>/thong-diep-tiep-thi</code>
          <p>Marketing messaging</p>
        </div>
        <div class="command-card">
          <code>/phan-tich-doi-thu</code>
          <p>Competitor analysis</p>
        </div>
      </div>
    </div>

    <!-- Sales -->
    <div class="category">
      <div class="category-header">
        <span class="category-icon">💰</span>
        <h2>Sales</h2>
        <span class="command-count">12 commands</span>
      </div>
      <div class="commands-grid">
        <div class="command-card">
          <code>/chien-luoc-ban-hang</code>
          <p>Sales strategies</p>
        </div>
        <div class="command-card">
          <code>/pheu-ban-hang</code>
          <p>Sales funnels</p>
        </div>
        <div class="command-card">
          <code>/khach-hang</code>
          <p>Customer profiles</p>
        </div>
        <div class="command-card">
          <code>/tao-bao-gia</code>
          <p>Generate quotes</p>
        </div>
        <div class="command-card">
          <code>/theo-doi-lead</code>
          <p>Lead tracking</p>
        </div>
        <div class="command-card">
          <code>/email-ban-hang</code>
          <p>Sales emails</p>
        </div>
      </div>
    </div>

    <!-- Operations -->
    <div class="category">
      <div class="category-header">
        <span class="category-icon">⚙️</span>
        <h2>Operations</h2>
        <span class="command-count">18 commands</span>
      </div>
      <div class="commands-grid">
        <div class="command-card">
          <code>/quan-ly-du-an</code>
          <p>Project management</p>
        </div>
        <div class="command-card">
          <code>/tao-workflow</code>
          <p>Workflow automation</p>
        </div>
        <div class="command-card">
          <code>/bao-cao-tien-do</code>
          <p>Progress reports</p>
        </div>
        <div class="command-card">
          <code>/quan-ly-doi</code>
          <p>Team management</p>
        </div>
        <div class="command-card">
          <code>/hop-dong</code>
          <p>Contract creation</p>
        </div>
        <div class="command-card">
          <code>/lich-hen</code>
          <p>Scheduling</p>
        </div>
      </div>
    </div>

    <!-- Finance -->
    <div class="category">
      <div class="category-header">
        <span class="category-icon">📊</span>
        <h2>Finance</h2>
        <span class="command-count">10 commands</span>
      </div>
      <div class="commands-grid">
        <div class="command-card">
          <code>/bao-cao-tai-chinh</code>
          <p>Financial reports</p>
        </div>
        <div class="command-card">
          <code>/hoa-don</code>
          <p>Invoice generation</p>
        </div>
        <div class="command-card">
          <code>/theo-doi-chi-phi</code>
          <p>Expense tracking</p>
        </div>
        <div class="command-card">
          <code>/du-bao-doanh-thu</code>
          <p>Revenue forecasting</p>
        </div>
        <div class="command-card">
          <code>/phan-tich-loi-nhuan</code>
          <p>Profit analysis</p>
        </div>
        <div class="command-card">
          <code>/ngan-sach</code>
          <p>Budgeting</p>
        </div>
      </div>
    </div>

    <!-- Strategy -->
    <div class="category">
      <div class="category-header">
        <span class="category-icon">🏯</span>
        <h2>Binh Pháp Strategy</h2>
        <span class="command-count">13 commands</span>
      </div>
      <div class="commands-grid">
        <div class="command-card">
          <code>/ke-hoach</code>
          <p>Strategic planning</p>
        </div>
        <div class="command-card">
          <code>/muu-cong</code>
          <p>Win-without-fighting</p>
        </div>
        <div class="command-card">
          <code>/hinh-the</code>
          <p>Positioning audit</p>
        </div>
        <div class="command-card">
          <code>/cuu-bien</code>
          <p>Pivot strategy</p>
        </div>
        <div class="command-card">
          <code>/hoa-cong</code>
          <p>Market disruption</p>
        </div>
        <div class="command-card">
          <code>/dung-gian</code>
          <p>Intelligence gathering</p>
        </div>
      </div>
    </div>

    <!-- AI Agents -->
    <div class="category">
      <div class="category-header">
        <span class="category-icon">🤖</span>
        <h2>17 AI Agents</h2>
        <span class="command-count">Specialized assistants</span>
      </div>
      <div class="commands-grid">
        <div class="command-card agent">
          <code>Scout Agent</code>
          <p>Market research</p>
        </div>
        <div class="command-card agent">
          <code>Editor Agent</code>
          <p>Content creation</p>
        </div>
        <div class="command-card agent">
          <code>Director Agent</code>
          <p>Video production</p>
        </div>
        <div class="command-card agent">
          <code>Community Agent</code>
          <p>Social distribution</p>
        </div>
        <div class="command-card agent">
          <code>Sales Agent</code>
          <p>Lead conversion</p>
        </div>
        <div class="command-card agent">
          <code>Finance Agent</code>
          <p>Financial tracking</p>
        </div>
      </div>
    </div>

  </section>

  <section class="features-cta">
    <h2>Ready to Automate Your Agency?</h2>
    <p>Start with 125+ commands today.</p>
    <div class="cta-buttons">
      <a href="/pricing" class="btn-primary">View Pricing</a>
      <a href="/docs/getting-started/installation" class="btn-secondary">Get Started</a>
    </div>
  </section>
</LandingLayout>

<style>
  /* ===================== HERO WOW ===================== */
  .features-hero {
    text-align: center;
    padding: 80px 24px 48px;
    animation: hero-entrance 0.8s ease-out;
  }

  @keyframes hero-entrance {
    from { opacity: 0; transform: translateY(-30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .features-hero h1 {
    font-size: 3rem;
    font-weight: 800;
    background: linear-gradient(135deg, #22c55e, #06b6d4, #8b5cf6, #22c55e);
    background-size: 300% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 16px;
    line-height: 1.2;
    animation: title-shine 5s linear infinite;
  }

  @keyframes title-shine {
    0% { background-position: 0% center; }
    100% { background-position: 300% center; }
  }

  .features-hero p {
    font-size: 1.25rem;
    color: var(--color-text-muted);
    animation: fade-up 0.6s ease-out 0.2s both;
  }

  @keyframes fade-up {
    from { opacity: 0; transform: translateY(15px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ===================== CATEGORIES ===================== */
  .features-categories {
    max-width: 1200px;
    margin: 0 auto;
    padding: 24px;
    display: flex;
    flex-direction: column;
    gap: 48px;
  }

  /* ===================== CATEGORY WOW ===================== */
  .category {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 32px;
    animation: category-entrance 0.6s ease-out both;
    transition: all 0.3s ease;
  }

  .category:nth-child(1) { animation-delay: 0.3s; }
  .category:nth-child(2) { animation-delay: 0.4s; }
  .category:nth-child(3) { animation-delay: 0.5s; }
  .category:nth-child(4) { animation-delay: 0.6s; }
  .category:nth-child(5) { animation-delay: 0.7s; }
  .category:nth-child(6) { animation-delay: 0.8s; }

  @keyframes category-entrance {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .category:hover {
    border-color: rgba(34, 197, 94, 0.3);
    transform: translateY(-4px);
    box-shadow: 0 12px 35px rgba(0, 0, 0, 0.1);
  }

  .category-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 24px;
    flex-wrap: wrap;
  }

  .category-icon {
    font-size: 2rem;
    animation: icon-float 3s ease-in-out infinite;
  }

  @keyframes icon-float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-4px); }
  }

  .category-header h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin: 0;
  }

  .command-count {
    background: var(--color-bg-tertiary);
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  .commands-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: 16px;
  }

  /* ===================== COMMAND CARDS WOW ===================== */
  .command-card {
    background: var(--color-bg-tertiary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 16px;
    transition: all 0.3s ease;
    animation: card-pop 0.4s ease-out both;
  }

  .commands-grid .command-card:nth-child(1) { animation-delay: 0.5s; }
  .commands-grid .command-card:nth-child(2) { animation-delay: 0.55s; }
  .commands-grid .command-card:nth-child(3) { animation-delay: 0.6s; }
  .commands-grid .command-card:nth-child(4) { animation-delay: 0.65s; }
  .commands-grid .command-card:nth-child(5) { animation-delay: 0.7s; }
  .commands-grid .command-card:nth-child(6) { animation-delay: 0.75s; }

  @keyframes card-pop {
    from { opacity: 0; transform: scale(0.9); }
    to { opacity: 1; transform: scale(1); }
  }

  .command-card:hover {
    border-color: #22c55e;
    transform: translateY(-4px) scale(1.02);
    box-shadow: 0 8px 20px rgba(34, 197, 94, 0.15);
  }

  .command-card.agent {
    border-color: rgba(139, 92, 246, 0.3);
  }

  .command-card.agent:hover {
    border-color: #a78bfa;
    box-shadow: 0 8px 20px rgba(139, 92, 246, 0.2);
  }

  .command-card code {
    display: block;
    font-size: 0.875rem;
    color: #22c55e;
    margin-bottom: 8px;
    font-family: 'JetBrains Mono', monospace;
    transition: all 0.2s ease;
  }

  .command-card:hover code {
    transform: translateX(2px);
  }

  .command-card.agent code {
    color: #8b5cf6;
  }

  .command-card p {
    font-size: 0.75rem;
    color: var(--color-text-muted);
    margin: 0;
  }

  /* ===================== CTA WOW ===================== */
  .features-cta {
    text-align: center;
    padding: 80px 24px;
    background: linear-gradient(180deg, transparent, rgba(34, 197, 94, 0.05));
    border-top: 1px solid var(--color-border);
    margin-top: 48px;
    animation: cta-entrance 0.6s ease-out 1s both;
  }

  @keyframes cta-entrance {
    from { opacity: 0; transform: scale(0.95); }
    to { opacity: 1; transform: scale(1); }
  }

  .features-cta h2 {
    font-size: 2rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin-bottom: 8px;
  }

  .features-cta p {
    color: var(--color-text-muted);
    margin-bottom: 24px;
  }

  .cta-buttons {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-primary {
    background: linear-gradient(135deg, #22c55e, #16a34a);
    color: white;
    padding: 14px 32px;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    animation: btn-pulse 2.5s ease-in-out infinite;
  }

  @keyframes btn-pulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.4); }
    50% { box-shadow: 0 0 20px 5px rgba(34, 197, 94, 0.2); }
  }

  .btn-primary:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 12px 30px rgba(34, 197, 94, 0.4);
  }

  .btn-secondary {
    background: var(--color-bg-tertiary);
    color: var(--color-text-primary);
    padding: 14px 32px;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    border: 1px solid var(--color-border);
    transition: all 0.3s ease;
  }

  .btn-secondary:hover {
    background: var(--color-bg-secondary);
    border-color: #22c55e;
    transform: translateY(-2px);
  }

  @media (max-width: 768px) {
    .features-hero h1 {
      font-size: 2rem;
    }

    .category {
      padding: 24px;
    }

    .commands-grid {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>
