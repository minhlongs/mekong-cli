---
import LandingLayout from '../layouts/LandingLayout.astro';

const title = 'Case Studies - AgencyOS';
const description = 'Real success stories from solo agencies using AgencyOS. See actual revenue results.';
---

<LandingLayout title={title} description={description}>
  <section class="cases-hero">
    <h1>📈 Success Stories</h1>
    <p>Real results from real solo agencies</p>
  </section>

  <section class="cases-grid">
    
    <!-- Case Study 1 -->
    <div class="case-card">
      <div class="case-badge">⭐ Featured</div>
      <div class="case-header">
        <span class="avatar">S</span>
        <div>
          <h3>Sarah K.</h3>
          <span class="role">Marketing Agency</span>
        </div>
      </div>
      
      <div class="case-metrics">
        <div class="metric">
          <span class="label">Before</span>
          <span class="value red">$3K/mo</span>
        </div>
        <div class="arrow">→</div>
        <div class="metric">
          <span class="label">After</span>
          <span class="value green">$15K/mo</span>
        </div>
        <div class="metric highlight">
          <span class="label">Growth</span>
          <span class="value">5x</span>
        </div>
      </div>

      <blockquote>
        "I replaced my entire team with AgencyOS. Not joking. My profit tripled in 3 months."
      </blockquote>

      <div class="case-details">
        <h4>Commands Used:</h4>
        <div class="tags">
          <code>/marketing/social</code>
          <code>/sales/proposal</code>
          <code>/video</code>
        </div>
      </div>

      <div class="case-timeline">
        <span>🗓️ 90 days to 5x revenue</span>
      </div>
    </div>

    <!-- Case Study 2 -->
    <div class="case-card">
      <div class="case-header">
        <span class="avatar blue">M</span>
        <div>
          <h3>Mike T.</h3>
          <span class="role">Freelancer → Agency</span>
        </div>
      </div>
      
      <div class="case-metrics">
        <div class="metric">
          <span class="label">Before</span>
          <span class="value red">$2K/mo</span>
        </div>
        <div class="arrow">→</div>
        <div class="metric">
          <span class="label">After</span>
          <span class="value green">$12K/mo</span>
        </div>
        <div class="metric highlight">
          <span class="label">Growth</span>
          <span class="value">6x</span>
        </div>
      </div>

      <blockquote>
        "From freelancer to agency owner. AgencyOS is the unfair advantage I needed."
      </blockquote>

      <div class="case-details">
        <h4>Commands Used:</h4>
        <div class="tags">
          <code>/sales/lead</code>
          <code>/sales/close</code>
          <code>/finance/invoice</code>
        </div>
      </div>

      <div class="case-timeline">
        <span>🗓️ 6 months to 6-figure agency</span>
      </div>
    </div>

    <!-- Case Study 3 -->
    <div class="case-card">
      <div class="case-header">
        <span class="avatar purple">T</span>
        <div>
          <h3>Trung V.</h3>
          <span class="role">Startup Consultant</span>
        </div>
      </div>
      
      <div class="case-metrics">
        <div class="metric">
          <span class="label">Deals/Month</span>
          <span class="value red">2</span>
        </div>
        <div class="arrow">→</div>
        <div class="metric">
          <span class="label">After</span>
          <span class="value green">8</span>
        </div>
        <div class="metric highlight">
          <span class="label">Growth</span>
          <span class="value">4x</span>
        </div>
      </div>

      <blockquote>
        "Binh Pháp strategy changed how I think about competition. Pure genius."
      </blockquote>

      <div class="case-details">
        <h4>Commands Used:</h4>
        <div class="tags">
          <code>/binh-phap</code>
          <code>/intel/competitor</code>
          <code>/fundraise</code>
        </div>
      </div>

      <div class="case-timeline">
        <span>🗓️ 4x deal flow in 60 days</span>
      </div>
    </div>

    <!-- Case Study 4 -->
    <div class="case-card">
      <div class="case-header">
        <span class="avatar gold">A</span>
        <div>
          <h3>Alex P.</h3>
          <span class="role">Affiliate Network</span>
        </div>
      </div>
      
      <div class="case-metrics">
        <div class="metric">
          <span class="label">Monthly Passive</span>
          <span class="value red">$0</span>
        </div>
        <div class="arrow">→</div>
        <div class="metric">
          <span class="label">After</span>
          <span class="value green">$2.5K</span>
        </div>
        <div class="metric highlight">
          <span class="label">ROI</span>
          <span class="value">25x</span>
        </div>
      </div>

      <blockquote>
        "Best $99 I ever spent. Franchise model is printing money for me."
      </blockquote>

      <div class="case-details">
        <h4>Commands Used:</h4>
        <div class="tags">
          <code>/affiliate/scout</code>
          <code>/marketing/social</code>
          <code>/video</code>
        </div>
      </div>

      <div class="case-timeline">
        <span>🗓️ Passive income in 30 days</span>
      </div>
    </div>

  </section>

  <section class="summary-section">
    <h2>📊 Aggregate Results</h2>
    <div class="summary-grid">
      <div class="summary-card">
        <span class="big-number">$40K+</span>
        <span class="label">Monthly Revenue Combined</span>
      </div>
      <div class="summary-card">
        <span class="big-number">5x</span>
        <span class="label">Average Growth</span>
      </div>
      <div class="summary-card">
        <span class="big-number">60</span>
        <span class="label">Days to Results</span>
      </div>
      <div class="summary-card">
        <span class="big-number">15+</span>
        <span class="label">Hours Saved/Week</span>
      </div>
    </div>
  </section>

  <section class="cta-section">
    <h2>Ready to be our next success story?</h2>
    <a href="/quickstart" class="btn-primary">Start Your Journey →</a>
  </section>
</LandingLayout>

<style>
  /* ===================== HERO WOW ===================== */
  .cases-hero {
    text-align: center;
    padding: 64px 24px 32px;
    animation: hero-entrance 0.8s ease-out;
  }

  @keyframes hero-entrance {
    from { opacity: 0; transform: translateY(-30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .cases-hero h1 {
    font-size: 3rem;
    font-weight: 800;
    background: linear-gradient(135deg, #22c55e, #06b6d4, #8b5cf6, #22c55e);
    background-size: 300% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 16px;
    animation: title-shine 5s linear infinite;
  }

  @keyframes title-shine {
    0% { background-position: 0% center; }
    100% { background-position: 300% center; }
  }

  .cases-hero p {
    font-size: 1.25rem;
    color: var(--color-text-muted);
    animation: fade-up 0.6s ease-out 0.2s both;
  }

  @keyframes fade-up {
    from { opacity: 0; transform: translateY(15px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ===================== CARDS GRID ===================== */
  .cases-grid {
    max-width: 1200px;
    margin: 0 auto;
    padding: 24px;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 24px;
  }

  /* ===================== CASE CARD WOW ===================== */
  .case-card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 24px;
    position: relative;
    transition: all 0.3s ease;
    animation: card-entrance 0.6s ease-out both;
  }

  .case-card:nth-child(1) { animation-delay: 0.3s; }
  .case-card:nth-child(2) { animation-delay: 0.4s; }
  .case-card:nth-child(3) { animation-delay: 0.5s; }
  .case-card:nth-child(4) { animation-delay: 0.6s; }

  @keyframes card-entrance {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .case-card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 24px;
    position: relative;
    transition: all 0.3s ease;
  }

  .case-card:hover {
    transform: translateY(-6px) scale(1.01);
    border-color: #22c55e;
    box-shadow: 0 15px 40px rgba(34, 197, 94, 0.2);
  }

  .case-badge {
    position: absolute;
    top: -10px;
    right: 16px;
    background: linear-gradient(135deg, #eab308, #f97316);
    color: white;
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 600;
    animation: badge-bounce 2s ease-in-out infinite;
  }

  @keyframes badge-bounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-3px); }
  }

  .case-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 20px;
  }

  .avatar {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: linear-gradient(135deg, #22c55e, #06b6d4);
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 700;
    font-size: 1.25rem;
  }

  .avatar.blue { background: linear-gradient(135deg, #3b82f6, #8b5cf6); }
  .avatar.purple { background: linear-gradient(135deg, #8b5cf6, #ec4899); }
  .avatar.gold { background: linear-gradient(135deg, #eab308, #f97316); }

  .case-header h3 {
    margin: 0;
    font-size: 1.125rem;
    color: var(--color-text-primary);
  }

  .role {
    font-size: 0.8rem;
    color: var(--color-text-muted);
  }

  .case-metrics {
    display: flex;
    align-items: center;
    gap: 8px;
    background: var(--color-bg-tertiary);
    padding: 12px;
    border-radius: 8px;
    margin-bottom: 16px;
    flex-wrap: wrap;
  }

  .metric {
    text-align: center;
  }

  .metric .label {
    display: block;
    font-size: 0.7rem;
    color: var(--color-text-muted);
    margin-bottom: 2px;
  }

  .metric .value {
    font-size: 1rem;
    font-weight: 700;
  }

  .metric .value.red { color: #ef4444; }
  .metric .value.green { color: #22c55e; }

  .metric.highlight {
    background: #22c55e;
    color: white;
    padding: 8px 12px;
    border-radius: 6px;
    margin-left: auto;
  }

  .metric.highlight .label { color: rgba(255,255,255,0.8); }
  .metric.highlight .value { color: white; font-size: 1.25rem; }

  .arrow {
    color: var(--color-text-muted);
    font-size: 1.25rem;
  }

  blockquote {
    font-style: italic;
    color: var(--color-text-secondary);
    border-left: 3px solid #22c55e;
    padding-left: 16px;
    margin: 0 0 16px 0;
    font-size: 0.9rem;
  }

  .case-details h4 {
    font-size: 0.75rem;
    color: var(--color-text-muted);
    margin: 0 0 8px 0;
    text-transform: uppercase;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }

  .tags code {
    background: var(--color-bg-tertiary);
    color: #22c55e;
    padding: 4px 8px;
    border-radius: 4px;
    font-size: 0.75rem;
  }

  .case-timeline {
    margin-top: 16px;
    padding-top: 16px;
    border-top: 1px solid var(--color-border);
    font-size: 0.8rem;
    color: var(--color-text-muted);
  }

  /* ===================== SUMMARY WOW ===================== */
  .summary-section {
    max-width: 900px;
    margin: 48px auto;
    padding: 24px;
    text-align: center;
    animation: fade-up 0.6s ease-out 0.7s both;
  }

  .summary-section h2 {
    font-size: 1.5rem;
    color: var(--color-text-primary);
    margin-bottom: 24px;
  }

  .summary-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 16px;
  }

  .summary-card {
    background: var(--color-bg-secondary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 20px;
    transition: all 0.3s ease;
    animation: stat-pop 0.4s ease-out both;
  }

  .summary-card:nth-child(1) { animation-delay: 0.8s; }
  .summary-card:nth-child(2) { animation-delay: 0.9s; }
  .summary-card:nth-child(3) { animation-delay: 1s; }
  .summary-card:nth-child(4) { animation-delay: 1.1s; }

  @keyframes stat-pop {
    from { opacity: 0; transform: scale(0.9); }
    to { opacity: 1; transform: scale(1); }
  }

  .summary-card:hover {
    transform: translateY(-4px);
    border-color: #22c55e;
  }

  .big-number {
    display: block;
    font-size: 2rem;
    font-weight: 800;
    color: #22c55e;
    margin-bottom: 4px;
  }

  .summary-card .label {
    font-size: 0.8rem;
    color: var(--color-text-muted);
  }

  /* ===================== CTA WOW ===================== */
  .cta-section {
    text-align: center;
    padding: 64px 24px;
    background: linear-gradient(180deg, transparent, rgba(34, 197, 94, 0.05));
    border-top: 1px solid var(--color-border);
    animation: fade-up 0.6s ease-out 1.2s both;
  }

  .cta-section h2 {
    font-size: 2rem;
    color: var(--color-text-primary);
    margin-bottom: 24px;
  }

  .btn-primary {
    display: inline-block;
    background: linear-gradient(135deg, #22c55e, #16a34a);
    color: white;
    padding: 14px 32px;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    animation: btn-pulse 2.5s ease-in-out infinite;
  }

  @keyframes btn-pulse {
    0%, 100% { box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.4); }
    50% { box-shadow: 0 0 20px 5px rgba(34, 197, 94, 0.2); }
  }

  .btn-primary:hover {
    transform: translateY(-3px) scale(1.02);
    box-shadow: 0 12px 30px rgba(34, 197, 94, 0.4);
  }

  @media (max-width: 768px) {
    .cases-hero h1 { font-size: 2rem; }
    .cases-grid { grid-template-columns: 1fr; }
  }
</style>
