---
import LandingLayout from '../../layouts/LandingLayout.astro';

const title = 'Marketing Assets | Affiliate Portal';
const description = 'Download banners, email templates, and social media assets to promote AgencyOS.';

const banners = [
  { size: '300x250', name: 'Medium Rectangle' },
  { size: '728x90', name: 'Leaderboard' },
  { size: '160x600', name: 'Wide Skyscraper' },
  { size: '320x100', name: 'Mobile Banner' },
];

const emails = [
  { name: 'Welcome Email', desc: '"I found this AI tool..."' },
  { name: 'Problem/Solution', desc: '"Tired of hiring VAs?"' },
  { name: 'Case Study', desc: '"How I saved $5k/month..."' },
  { name: 'Limited Time', desc: '"50% off this week only"' },
  { name: 'Comparison', desc: '"AgencyOS vs hiring team"' },
];
---

<LandingLayout title={title} description={description}>
  <div class="assets-container">
    <header class="page-header">
      <a href="/affiliate/dashboard" class="back-link">← Back to Dashboard</a>
      <h1>🎨 Marketing Assets</h1>
      <p>Professional materials to promote AgencyOS</p>
    </header>

    <!-- Banner Section -->
    <section class="asset-section">
      <div class="section-header">
        <h2>📱 Banners</h2>
        <span class="count">{banners.length} sizes</span>
      </div>
      <div class="banners-grid">
        {banners.map(banner => (
          <div class="banner-card">
            <div class="banner-preview">
              <div class="placeholder-banner" style={`aspect-ratio: ${banner.size.replace('x', '/')};`}>
                <span>🏯 AgencyOS</span>
                <span class="size">{banner.size}</span>
              </div>
            </div>
            <div class="banner-info">
              <div class="banner-name">{banner.name}</div>
              <div class="banner-size">{banner.size}px</div>
            </div>
            <button class="download-btn">
              ⬇️ Download
            </button>
          </div>
        ))}
      </div>
    </section>

    <!-- Email Templates -->
    <section class="asset-section">
      <div class="section-header">
        <h2>📧 Email Swipes</h2>
        <span class="count">{emails.length} templates</span>
      </div>
      <div class="emails-grid">
        {emails.map((email, i) => (
          <div class="email-card">
            <div class="email-number">#{i + 1}</div>
            <div class="email-content">
              <div class="email-name">{email.name}</div>
              <div class="email-preview">{email.desc}</div>
            </div>
            <button class="copy-btn">📋 Copy</button>
          </div>
        ))}
      </div>
    </section>

    <!-- Social Media Templates -->
    <section class="asset-section">
      <div class="section-header">
        <h2>📱 Social Media</h2>
        <span class="count">Ready to use</span>
      </div>
      <div class="social-grid">
        <div class="social-card twitter">
          <div class="platform">𝕏 Twitter/X Thread</div>
          <pre class="social-content">🧵 I just found the ultimate one-person agency tool...

1/ 125+ AI commands for marketing, sales, finance
2/ Costs less than one VA hour per month
3/ Works in Vietnamese AND English

Here's my honest review 👇

[YOUR LINK]</pre>
          <button class="copy-btn">📋 Copy Thread</button>
        </div>
        
        <div class="social-card tiktok">
          <div class="platform">🎬 TikTok/Reels Script</div>
          <pre class="social-content">"POV: You run an agency but have zero employees"

*Shows AgencyOS in action*

"125 AI commands. One subscription."

*Cut to dashboard*

"Link in bio"</pre>
          <button class="copy-btn">📋 Copy Script</button>
        </div>

        <div class="social-card linkedin">
          <div class="platform">💼 LinkedIn Post</div>
          <pre class="social-content">I've been running my agency solo for 6 months now.

Zero employees. Zero VAs.

Just me and 125 AI commands.

Here's what I learned:

→ AI handles 80% of repetitive tasks
→ Marketing runs on autopilot
→ Client work gets done faster

The tool? AgencyOS.

Worth checking out if you're a solo operator.

#solopreneur #aitools #agency</pre>
          <button class="copy-btn">📋 Copy Post</button>
        </div>
      </div>
    </section>

    <!-- Video Assets -->
    <section class="asset-section">
      <div class="section-header">
        <h2>🎬 Video Promos</h2>
        <span class="count">Coming soon</span>
      </div>
      <div class="coming-soon">
        <span class="icon">🎬</span>
        <p>Professional video promos coming soon!</p>
        <p class="hint">15s, 30s, and 60s versions</p>
      </div>
    </section>

    <!-- Brand Guidelines -->
    <section class="asset-section">
      <div class="section-header">
        <h2>📐 Brand Guidelines</h2>
      </div>
      <div class="brand-grid">
        <div class="brand-card">
          <h3>Colors</h3>
          <div class="color-swatches">
            <div class="swatch" style="background: #10b981;">
              <span>#10b981</span>
            </div>
            <div class="swatch" style="background: #059669;">
              <span>#059669</span>
            </div>
            <div class="swatch" style="background: #0f172a;">
              <span>#0f172a</span>
            </div>
          </div>
        </div>
        <div class="brand-card">
          <h3>Logo</h3>
          <div class="logo-preview">🏯 AgencyOS</div>
          <p class="hint">Always use the castle emoji</p>
        </div>
        <div class="brand-card">
          <h3>Tagline</h3>
          <div class="tagline">"125+ AI Commands. Zero Employees."</div>
        </div>
      </div>
    </section>
  </div>
</LandingLayout>

<style>
  .assets-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 100px 20px 60px;
  }

  .back-link {
    color: var(--color-text-secondary);
    text-decoration: none;
    display: inline-block;
    margin-bottom: 16px;
  }

  .back-link:hover {
    color: #10b981;
  }

  .page-header {
    margin-bottom: 40px;
  }

  .page-header h1 {
    font-size: 2.5rem;
    margin-bottom: 8px;
  }

  .page-header p {
    color: var(--color-text-secondary);
  }

  .asset-section {
    margin-bottom: 50px;
  }

  .section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
  }

  .section-header h2 {
    font-size: 1.5rem;
    margin: 0;
  }

  .count {
    background: var(--color-bg-tertiary);
    padding: 6px 12px;
    border-radius: 20px;
    font-size: 0.85rem;
    color: var(--color-text-secondary);
  }

  .banners-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
  }

  .banner-card {
    background: var(--color-bg-tertiary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 20px;
    text-align: center;
  }

  .placeholder-banner {
    background: linear-gradient(135deg, #10b981, #059669);
    border-radius: 8px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 700;
    margin-bottom: 16px;
    max-height: 150px;
    transition: transform 0.3s ease;
  }

  .banner-card:hover .placeholder-banner {
    transform: scale(1.05);
  }

  .placeholder-banner .size {
    font-size: 0.75rem;
    opacity: 0.8;
    margin-top: 4px;
  }

  .banner-name {
    font-weight: 600;
    margin-bottom: 4px;
  }

  .banner-size {
    font-size: 0.85rem;
    color: var(--color-text-secondary);
    margin-bottom: 12px;
  }

  .download-btn, .copy-btn {
    padding: 10px 20px;
    background: var(--color-bg-primary);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    color: var(--color-text-primary);
    cursor: pointer;
    font-size: 0.9rem;
    transition: all 0.3s ease;
    animation: button-glow 2.5s ease-in-out infinite;
  }

  @keyframes button-glow {
    0%, 100% { box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.4); }
    50% { box-shadow: 0 0 15px 3px rgba(16, 185, 129, 0.2); }
  }

  .download-btn:hover, .copy-btn:hover {
    border-color: #10b981;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(16, 185, 129, 0.3);
  }

  .emails-grid {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  /* ===================== EMAIL CARDS WOW ===================== */
  .email-card {
    background: var(--color-bg-tertiary);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 20px;
    display: flex;
    align-items: center;
    gap: 16px;
    animation: slide-right 0.5s ease-out both;
    transition: all 0.3s ease;
  }

  .email-card:nth-child(1) { animation-delay: 0.1s; }
  .email-card:nth-child(2) { animation-delay: 0.2s; }
  .email-card:nth-child(3) { animation-delay: 0.3s; }
  .email-card:nth-child(4) { animation-delay: 0.4s; }
  .email-card:nth-child(5) { animation-delay: 0.5s; }

  @keyframes slide-right {
    from { opacity: 0; transform: translateX(-20px); }
    to { opacity: 1; transform: translateX(0); }
  }

  .email-card:hover {
    transform: translateX(4px);
    border-color: #10b981;
  }

  .email-number {
    background: linear-gradient(135deg, #10b981, #059669);
    color: white;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
  }

  .email-content {
    flex: 1;
  }

  .email-name {
    font-weight: 600;
    margin-bottom: 4px;
  }

  .email-preview {
    color: var(--color-text-secondary);
    font-size: 0.9rem;
  }

  .social-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }

  /* ===================== SOCIAL CARDS WOW ===================== */
  .social-card {
    background: var(--color-bg-tertiary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 24px;
    animation: card-pop 0.5s ease-out both;
    transition: all 0.3s ease;
  }

  .social-card:nth-child(1) { animation-delay: 0.2s; }
  .social-card:nth-child(2) { animation-delay: 0.3s; }
  .social-card:nth-child(3) { animation-delay: 0.4s; }

  .social-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 12px 30px rgba(16, 185, 129, 0.15);
    border-color: #10b981;
  }

  .platform {
    font-weight: 600;
    margin-bottom: 16px;
    font-size: 1.1rem;
  }

  .social-content {
    background: var(--color-bg-primary);
    padding: 16px;
    border-radius: 8px;
    font-size: 0.85rem;
    white-space: pre-wrap;
    margin-bottom: 16px;
    max-height: 200px;
    overflow-y: auto;
    font-family: inherit;
    color: var(--color-text-secondary);
  }

  .coming-soon {
    background: var(--color-bg-tertiary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 60px;
    text-align: center;
    color: var(--color-text-secondary);
  }

  .coming-soon .icon {
    font-size: 3rem;
    display: block;
    margin-bottom: 16px;
  }

  .hint {
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  .brand-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }

  .brand-card {
    background: var(--color-bg-tertiary);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 24px;
  }

  .brand-card h3 {
    margin-bottom: 16px;
    font-size: 1rem;
  }

  .color-swatches {
    display: flex;
    gap: 12px;
  }

  .swatch {
    width: 60px;
    height: 60px;
    border-radius: 12px;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding-bottom: 4px;
    animation: swatch-pulse 3s ease-in-out infinite;
  }

  @keyframes swatch-pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }

  .swatch span {
    font-size: 0.6rem;
    color: white;
    text-shadow: 0 1px 2px rgba(0,0,0,0.5);
  }

  .logo-preview {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 8px;
  }

  .tagline {
    font-size: 1.1rem;
    font-style: italic;
    color: var(--color-text-secondary);
  }

  @media (max-width: 1024px) {
    .banners-grid, .social-grid, .brand-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .banners-grid, .social-grid, .brand-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
