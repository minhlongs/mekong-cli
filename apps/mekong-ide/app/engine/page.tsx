import { EngineFarmPage } from "@/components/engines";

/**
 * Engine Farm Monitor route — /engine
 * Renders the full engine farm page with cards, queue/router metrics, and system resources.
 */
export default function EnginePage() {
  return <EngineFarmPage />;
}
