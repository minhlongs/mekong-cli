/**
 * Re-export wrapper — canonical source is @agencyos/trading-core/arbitrage.
 * Kept for backward compatibility with existing test imports.
 */
export { ArbitrageExecutor, type ExecutionResult, type ArbitrageTradeLog, type PnLDashboard, type ExecutorConfig } from '@agencyos/trading-core/arbitrage';
