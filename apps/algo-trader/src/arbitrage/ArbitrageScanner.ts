/**
 * Re-export wrapper — canonical source is @agencyos/trading-core/arbitrage.
 * Kept for backward compatibility with existing test imports.
 */
export { ArbitrageScanner, type ArbitrageOpportunity, type ScannerConfig, type ScannerStats, type ExchangePrice } from '@agencyos/trading-core/arbitrage';
