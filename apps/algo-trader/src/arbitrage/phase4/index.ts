/**
 * Phase 4 barrel exports — Polymarket Sentinel, Sybil Mirage, Shadow Layering.
 * Uses `export type` for interface-only re-exports (isolatedModules compliance).
 * SIMULATION MODE ONLY — no real funds or mainnet connections.
 */

export { Phase4Orchestrator } from './phase4-orchestrator';
export type { Phase4Config, Phase4Status, Phase4WsMessage } from './phase4-orchestrator';
