{
  "name": "@mekong/observability",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "build": "echo 'No build step needed - using ESM directly'",
    "test": "vitest run --poolOptions.forks.maxForks=2",
    "test:watch": "vitest --poolOptions.forks.maxForks=2",
    "lint": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
