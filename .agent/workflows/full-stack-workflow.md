---
description: Full-Stack Product Development using Binh Pháp methodology - from idea to production in one day
---
// turbo-all

# Full-Stack Product Development Workflow

Build a complete SaaS product using AgencyOS following the 13 Binh Pháp clusters.

## The Pipeline

```
┌────────────────────────────────────────────────────────────────────┐
│                   BINH PHÁP PRODUCT WORKFLOW                       │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  Phase 1          Phase 2         Phase 3         Phase 4         │
│  STRATEGIZE       BUILD           SHIP            OPTIMIZE        │
│  ──────────       ─────           ────            ────────        │
│  /binh-phap       /cook           /ship           /marketing      │
│  /plan            /test           /deploy         /intel          │
│                                   /monitor                        │
│                                                                    │
│  Clusters 1-3     Clusters 4-7    Clusters 8-11   Clusters 12-13  │
│  Kế Hoạch         Hình Thế        Cửu Biến        Hỏa Công        │
│  Tác Chiến        Thế Trận        Hành Quân       Dụng Gián       │
│  Mưu Công         Hư Thực         Địa Hình                        │
│                   Quân Tranh      Cửu Địa                         │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

## Phase 1: Strategic Analysis (2 hours)

### Step 1.1: Binh Pháp Analysis
```bash
python3 cli/main.py binh-phap "I want to build a newsletter SaaS for agencies"
```

### Step 1.2: Create Implementation Plan
```bash
python3 cli/main.py plan "Create detailed implementation plan based on analysis"
```

## Phase 2: Building (4 hours)

### Step 2.1: Build Authentication
```bash
python3 cli/main.py cook "Build user authentication with email and OAuth"
```

### Step 2.2: Build Core Features
```bash
python3 cli/main.py cook "Build newsletter editor with drag-drop blocks"
```

### Step 2.3: Build Dashboard
```bash
python3 cli/main.py cook "Build analytics dashboard with charts"
```

### Step 2.4: Run Tests
```bash
python3 cli/main.py test
```

## Phase 3: Shipping (2 hours)

### Step 3.1: Deploy to Production
```bash
python3 cli/main.py ship "Deploy to production with custom domain"
```

### Step 3.2: Set Up Monitoring
```bash
python3 cli/main.py monitor
```

## Phase 4: Optimize (Ongoing)

### Step 4.1: Launch Marketing Campaign
```bash
python3 cli/main.py marketing "Create viral launch campaign"
```

### Step 4.2: Gather Intelligence
```bash
python3 cli/main.py intel "Monitor competitors and gather feedback"
```

## Complete Example: One-Day Build

```bash
# Morning: Strategy (2 hours)
python3 cli/main.py binh-phap "newsletter SaaS for agencies"
python3 cli/main.py plan "create implementation plan"

# Afternoon: Build (4 hours)
python3 cli/main.py cook "build auth with email + OAuth"
python3 cli/main.py cook "build newsletter editor"
python3 cli/main.py cook "build analytics dashboard"
python3 cli/main.py test

# Evening: Ship (2 hours)
python3 cli/main.py ship "deploy to production"
python3 cli/main.py marketing "create launch campaign"
python3 cli/main.py intel "set up monitoring"

# Total: 8 hours to production SaaS
```

## 🏯 Binh Pháp Wisdom

> "Bách chiến bách thắng, phi thiện chi thiện giả dã"
> *100 battles, 100 wins - not the greatest victory*

> "Bất chiến nhi khuất nhân chi binh, thiện chi thiện giả dã"
> *Win without fighting - the greatest victory*

## WIN-WIN-WIN Alignment

| Stakeholder | WIN |
|-------------|-----|
| 👑 Anh | Complete product in one day |
| 🏢 Agency | Repeatable process for clients |
| 🚀 Startup | Fast time-to-market |
