# Phase 4 — .orchestrate/ Gitignore

**Goal:** Prevent pipeline artifacts from being tracked.

## Changes

- Added `.orchestrate/` to `.gitignore` after `.archive/`

## Acceptance

- `.orchestrate/` directory untracked
