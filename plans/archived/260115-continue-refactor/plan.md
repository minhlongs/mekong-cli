---
title: "Continuous .claude Architecture Refactoring"
description: "Systematic cleanup and optimization of remaining .claude assets (agents, commands, rules, skills) to align with AgencyOS v2.5 standards."
status: pending
priority: P1
effort: 6h
branch: refactor/claude-assets
tags: [refactor, claude, agents, commands, skills]
created: 2026-01-15
---

# 🧹 Chiến Lược: Dọn Dẹp & Chuẩn Hóa .claude

> **Mục tiêu:** Tiếp tục rà soát và chuẩn hóa toàn bộ các file còn lại trong `.claude/` để đảm bảo không còn file rác, file cũ, hoặc file không tuân thủ kiến trúc Binh Pháp.

## 📋 Lộ Trình Thực Thi (Execution Tasks)

- [ ] **Phase 1: Agents Refactoring (Lục Tướng mở rộng)**
  - Review và chuẩn hóa các agent hỗ trợ: `code-reviewer`, `tester`, `researcher`.
  - Loại bỏ hoặc gộp các agent thừa: `scout-external` (gộp vào `researcher`), `client-value`, `deal-closer` (gộp vào `money-maker` hoặc `client-magnet`).
  - Cập nhật prompt system cho từng agent để nhận diện `antigravity` core.

- [ ] **Phase 2: Commands Cleanup (Slash Commands)**
  - Xóa sạch các thư mục con trong `.claude/commands/` nếu chúng chứa file rác (đã xóa file `.md` ở bước trước nhưng chưa xóa thư mục rỗng).
  - Chuẩn hóa `scout.md` và `test.md`.

- [ ] **Phase 3: Skills Optimization (Vũ Khí)**
  - Rà soát các skill kỹ thuật: `backend-development`, `frontend-development`, `devops`.
  - Đảm bảo chúng trỏ đúng vào stack "Solo" (Supabase + Vercel).
  - Xóa các skill quá cũ hoặc không dùng (ví dụ: `google-adk-python` nếu không dùng, `threejs` nếu không phải core).

- [ ] **Phase 4: Rules Alignment (Quân Luật)**
  - Cập nhật `development-rules.md` để ép buộc sử dụng `plans/` và `tests/`.
  - Xóa `orchestration-protocol.md` nếu đã tích hợp vào `VIBEWorkflow`.

## 🔍 Context
- **Target:** `.claude/` directory.
- **Standard:** Agency OS v2.5 Architecture.

---
*Generated by Planner Agent | Binh Pháp: Mưu Công*
