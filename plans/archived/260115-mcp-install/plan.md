---
title: "Zero-Prompt MCP Installation"
description: "Enable direct installation of MCP servers via CLI commands without conversational overhead."
status: pending
priority: P1
effort: 2h
branch: feat/mcp-install-cmd
tags: [mcp, cli, automation]
created: 2026-01-15
---

# 🔌 Chiến Lược: Zero-Prompt MCP Installation

> **Mục tiêu:** Cho phép người dùng cài đặt các MCP Server (như `supabase-mcp`) chỉ bằng một câu lệnh CLI duy nhất, bỏ qua các bước hội thoại rườm rà.

## 📋 Lộ Trình Thực Thi (Execution Tasks)

- [ ] **Phase 1: Analysis & Design**
  - Nghiên cứu cấu trúc `supabase-mcp`.
  - Thiết kế lệnh `mekong mcp install <url>`.
  - Xác định cách tự động cấu hình `.claude/mcp.json` (hoặc tương đương).

- [ ] **Phase 2: MCP Manager Enhancement**
  - Cập nhật `antigravity/core/mcp_manager.py` (nếu chưa có thì tạo mới, hoặc cập nhật `core/config.py` logic).
  - Thêm logic `git clone`, `npm install`/`pip install`, và update config.

- [ ] **Phase 3: CLI Integration**
  - Thêm lệnh vào `cli/main.py` (hoặc `cli/config.py` được gọi bởi main).
  - Xử lý các biến môi trường cần thiết (ví dụ: `SUPABASE_URL`, `SUPABASE_KEY`).

- [ ] **Phase 4: Testing & Shipping**
  - Test cài đặt `https://github.com/supabase-community/supabase-mcp`.
  - Commit và push.

## 🔍 Context
- **Target:** `https://github.com/supabase-community/supabase-mcp`
- **Current Config:** `.claude/.mcp.json.example` (cần kiểm tra file thực tế đang dùng).

---
*Generated by Planner Agent*
