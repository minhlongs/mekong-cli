# v0.2 Type Unification Report

## Summary
Unified `Plan`, `Step`, and `PlanStatus` type definitions into `src/core/protocols.py` as the single canonical source. Removed duplicate definitions from `src/core/runtime_adapter.py` and `src/core/goal_engine.py`.

## Changes

### src/core/protocols.py
- Added imports: `dataclass`, `field` (dataclasses), `Enum` (enum)
- Replaced `Plan` Protocol (lines 76-80) with three concrete dataclasses:
  - `PlanStatus(str, Enum)` — PENDING, IN_PROGRESS, COMPLETED, FAILED
  - `Step` — dataclass with `id`, `description`, `dependencies`, `params`
  - `Plan` — dataclass with `id`, `goal`, `steps`, `status`, `metadata`
- Updated `__all__` to export `PlanStatus`, `Step`, `Plan`

### src/core/runtime_adapter.py
- Added import: `from src.core.protocols import Plan, PlanStatus, Step`
- Removed duplicate `PlanStatus` Enum definition
- Removed duplicate `Step` dataclass definition
- Removed duplicate `Plan` dataclass definition
- Fixed `Plan` construction: changed `goal_id=goal.id` to `goal=goal.id` to match canonical `Plan.goal` field name

### src/core/goal_engine.py
- Added import: `from src.core.protocols import Plan, PlanStatus, Step`
- Removed duplicate `PlanStatus` Enum definition
- Removed duplicate `Step` dataclass definition
- Removed duplicate `Plan` dataclass definition
- Removed unused `Enum` import

## Verification
- ruff check: PASS (src/core/protocols.py, src/core/runtime_adapter.py, src/core/goal_engine.py)
- pytest tests/test_protocol_compliance.py: 5/5 PASSED
- pytest tests/test_governance.py: 29/29 PASSED
- Import verification: all three modules import and instantiate correctly

## Files Modified
- src/core/protocols.py
- src/core/runtime_adapter.py
- src/core/goal_engine.py