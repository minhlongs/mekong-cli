{
  "id": "data-full-refresh",
  "version": "1.0.0",
  "layer": "engineering",
  "chapter": "軍形 — Disposition of Forces",
  "display": {
    "name_en": "Full Data Refresh",
    "icon": "",
    "description_en": "Full refresh — ingest, transform, quality, catalog in parallel, then metrics"
  },
  "execution": {
    "agents": ["data-engineer", "analytics-engineer"],
    "hub": "data-hub",
    "complexity": "complex",
    "timeout_ms": 300000,
    "credit_cost": 13,
    "requires_approval": false
  },
  "input": {
    "type": "object",
    "required": ["goal"],
    "properties": {
      "goal": { "type": "string", "description": "Natural language task" }
    }
  },
  "output": {
    "type": "object",
    "properties": {
      "result": { "type": "string" },
      "artifacts": { "type": "array", "items": { "type": "string" } }
    }
  },
  "cascade": { "triggers": [] },
  "validation": {
    "output_must_contain": [],
    "min_output_length": 100,
    "max_credit_cost": 15
  }
}
