{
  "id": "ipo-board-prep",
  "version": "1.0.0",
  "layer": "founder",
  "chapter": "用間 — Use of Intelligence",
  "display": {
    "name_en": "IPO Board Prep",
    "icon": "",
    "description_en": "Board prep — meeting management, metrics, risk report in parallel, then minutes"
  },
  "execution": {
    "agents": ["corporate-secretary", "ir-lead", "risk-analyst"],
    "hub": "governance-hub",
    "complexity": "complex",
    "timeout_ms": 300000,
    "credit_cost": 11,
    "requires_approval": false
  },
  "input": {
    "type": "object",
    "required": ["goal"],
    "properties": {
      "goal": { "type": "string", "description": "Natural language task" }
    }
  },
  "output": {
    "type": "object",
    "properties": {
      "result": { "type": "string" },
      "artifacts": { "type": "array", "items": { "type": "string" } }
    }
  },
  "cascade": { "triggers": [] },
  "validation": {
    "output_must_contain": [],
    "min_output_length": 100,
    "max_credit_cost": 15
  }
}
