{
  "id": "revops-handoff",
  "version": "1.0.0",
  "layer": "business",
  "chapter": "軍爭 — Armed Struggle",
  "display": {
    "name_en": "Lead Handoff",
    "icon": "",
    "description_en": "SLA-governed lead and opportunity handoff"
  },
  "execution": {
    "agents": ["rev-ops-analyst"],
    "hub": "revops-hub",
    "complexity": "simple",
    "timeout_ms": 120000,
    "credit_cost": 2,
    "requires_approval": false
  },
  "input": {
    "type": "object",
    "required": ["goal"],
    "properties": {
      "goal": { "type": "string", "description": "Natural language task" }
    }
  },
  "output": {
    "type": "object",
    "properties": {
      "result": { "type": "string" },
      "artifacts": { "type": "array", "items": { "type": "string" } }
    }
  },
  "cascade": { "triggers": [] },
  "validation": {
    "output_must_contain": [],
    "min_output_length": 50,
    "max_credit_cost": 5
  }
}
