{
  "id": "compliance-soc2-prep",
  "version": "1.0.0",
  "layer": "ops",
  "chapter": "行軍 — Army on the March",
  "display": {
    "name_en": "SOC2 Preparation",
    "icon": "",
    "description_en": "SOC2 prep — audit, policy, IAM review, compliance report"
  },
  "execution": {
    "agents": ["compliance-officer", "security-engineer", "iam-admin"],
    "hub": "compliance-hub",
    "complexity": "complex",
    "timeout_ms": 300000,
    "credit_cost": 13,
    "requires_approval": false
  },
  "input": {
    "type": "object",
    "required": ["goal"],
    "properties": {
      "goal": { "type": "string", "description": "Natural language task" }
    }
  },
  "output": {
    "type": "object",
    "properties": {
      "result": { "type": "string" },
      "artifacts": { "type": "array", "items": { "type": "string" } }
    }
  },
  "cascade": { "triggers": [] },
  "validation": {
    "output_must_contain": [],
    "min_output_length": 100,
    "max_credit_cost": 15
  }
}
