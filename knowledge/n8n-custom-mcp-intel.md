# n8n Custom MCP Setup (Mưu Công)
Date: 2026-02-23

## Status
- **Architecture**: Mapped out Docker-compose structure (n8n + PostgreSQL + Redis + custom-mcp server).
- **Tooling**: Enables `n8n` workflow creation, execution, and debugging purely through MCP boundaries without opening the browser.

## Binh Pháp Mapping
**謀攻 MƯU CÔNG (Không đánh mà thắng)**: The highest form of warfare is breaking the enemy's resistance without fighting. By giving Claude Code explicit permission to construct and run n8n pipelines via MCP endpoints, we eliminate manual UI drag-and-drop operations—the AI codes its own visual workflows.

*(Due to the requirement of active Docker footprint and PostgreSQL databases, the live installation is bypassed at this reconnaissance stage, with the architecture cataloged).*
